---
name: "speckit-git-remote"
description: "Codex wrapper for the Speckit git remote workflow."
---

# speckit-git-remote

Slash label: `/speckit.git.remote`.

Use `../../../.agents/skills/speckit-git-remote/SKILL.md` as the source workflow.

Rules:
- Read the source skill before doing anything else.
- Pass any text after `/speckit.git.remote` through as the original command input for the source skill.
- Execute the source skill exactly, including remote detection behavior.
