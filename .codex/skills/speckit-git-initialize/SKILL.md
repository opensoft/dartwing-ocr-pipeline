---
name: "speckit-git-initialize"
description: "Codex wrapper for the Speckit git initialization workflow."
---

# speckit-git-initialize

Slash label: `/speckit.git.initialize`.

Use `../../../.agents/skills/speckit-git-initialize/SKILL.md` as the source workflow.

Rules:
- Read the source skill before doing anything else.
- Pass any text after `/speckit.git.initialize` through as the original command input for the source skill.
- Execute the source skill exactly, including repository initialization behavior.
