[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ledgerlinc-ocr"
version = "0.1.0"
description = "LedgerLinc Stage 1 OCR pipeline: contracts and validator for vendor-identity artifacts."
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.22,<5",
    "pydantic>=2.7,<3",
    "pypdfium2>=4.30,<5",
    "paddleocr>=2.8,<3",
    "paddlepaddle>=3.0,<4",
    "Pillow>=10.4,<11",
    "numpy>=1.26,<3",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2,<9",
    "pypdf>=5.0,<7",
    "pytest-socket>=0.7,<1",
]

[project.scripts]
ledgerlinc-preprocess = "ledgerlinc_ocr.preprocessing.cli:main"
ledgerlinc-pipeline = "ledgerlinc_ocr.pipeline.cli:main"
ledgerlinc-evidence-packet = "ledgerlinc_ocr.evidence_packet.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
include = ["ledgerlinc_ocr*"]

[tool.pytest.ini_options]
testpaths = ["tests/contract_tests", "tests/pipeline_tests", "tests/unit", "tests/integration"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-ra"
