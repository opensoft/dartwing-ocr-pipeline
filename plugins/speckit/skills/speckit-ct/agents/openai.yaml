interface:
  display_name: "/ct"
  short_description: "Show the latest Speckit worktree"
  default_prompt: "Use $speckit-ct to resolve the latest Speckit worktree handoff."
policy:
  allow_implicit_invocation: false
