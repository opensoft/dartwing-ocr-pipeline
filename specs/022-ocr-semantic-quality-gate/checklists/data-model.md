# Data Model Requirements Quality Checklist: OCR Semantic Quality Gate

> **Historical-vocabulary note (added 2026-05-23)**: Resolution notes on closed `[x]` items below predate the 2026-05-23 F1–F12 remediation. References to `evaluation_document_writer.py` / `run_summary_serializer.py`, `failed_categories`, or `reason_text` reflect the artifact state at the time the item was resolved — they are NOT current vocabulary. Post-remediation pinnings: `spec.md` FR-017/FR-018, `data-model.md` §5/§7/§8, `contracts/module-invariants.md` MI-14; summary in `alignment-verification.md`.

**Purpose**: Validate that the data-model requirements for all Key Entities are complete, clear, consistent, measurable, and free of gaps that would prevent unambiguous implementation or testing.
**Created**: 2026-05-22
**Feature**: [spec.md](../spec.md)

---

## Entity Completeness

- [x] CHK001 Does the spec fully enumerate every top-level field of `semantic_table_truth.json` beyond `document_id` and the rows array — e.g. a schema version field, a corpus root identifier, or a creation timestamp — or is the complete sidecar envelope shape left unspecified? [Completeness, Gap] — ⚠ OPEN: spec names only `document_id` + rows; no envelope schema version or other top-level fields specified — left for JSON Schema authoring — ✓✓ Data-Model §1 (top-level field table: `document_id`, `rows`, optional `schema_version`; `additionalProperties: false`); Schema-Amendments §semantic_table_truth.schema.json (verbatim JSON Schema with exactly these three properties)
- [x] CHK002 Is the exact JSON key name for the rows array within `semantic_table_truth.json` specified in FR-001 or the Key Entities section, or is it left for the implementer to choose? [Completeness, Spec §FR-001] — ⚠ OPEN: no JSON key name for the rows array is given in FR-001, FR-002, or Key Entities — ✓✓ Data-Model §1 (field table names the key `rows`); Schema-Amendments §semantic_table_truth.schema.json (`"rows"` is the exact JSON property name)
- [x] CHK003 Does the spec define whether `semantic_table_truth.json` must declare a minimum number of rows (other than the zero-row edge case rejecting it), or is any positive row count equally valid? [Completeness, Spec §Edge Cases] — ⚠ OPEN: spec only rejects zero-row sidecars; "any positive count" is implied but not explicit — ✓✓ Schema-Amendments §semantic_table_truth.schema.json (`"minItems": 1` on the rows array; any positive row count is valid); Data-Model §1 (`rows` field: "Non-empty")
- [x] CHK004 Is the full shape of the `semantic_table_quality` object — every required and optional field — defined precisely enough to write a JSON Schema, or does FR-017 leave the internal structure of that object to be inferred? [Completeness, Spec §FR-017] — ✓ FR-017 / Q19: FR-017 enumerates `status`, `failed_checks`, `row_reasons`, and optional `supporting_evidence`; FR-015 and FR-018 add field-level detail
- [x] CHK005 Is the complete shape of the aggregate semantic-quality fields added to `evaluation_run_summary.json` specified with field names, types, and semantics, or does FR-018 name the concept without specifying the JSON structure? [Completeness, Spec §FR-018] — ✓ FR-018 / Q19: FR-018 enumerates all 8 `semantic_table_quality_metrics` field names with types and semantics, plus per-document status entry fields
- [x] CHK006 Is the exact structure of a `Failed Check` record — beyond its category and row identifier — specified, including what evidence fields it must carry so a reviewer can understand the cause without re-running the gate? [Completeness, Spec §FR-015] — ✓ FR-015 / Q17: FR-015 mandates `category`, `row_id`, applicable sidecar field or row-text token, expected value/text, observed raw token or normalized absence/alignment finding, and the check predicate applied
- [x] CHK007 Is the per-document semantic status entry in `evaluation_run_summary.json` (referenced in FR-018) defined with field names and value types, or only described in prose? [Completeness, Spec §FR-018] — ✓ FR-018 / Q19: FR-018 enumerates `document_id`, `semantic_table_quality_status`, and `semantic_table_quality_passed` as the three required per-document status entry fields
- [x] CHK008 Does the spec specify what supporting-evidence fields accompany the OCR detector confidence in the Semantic Quality Verdict entity, or only that confidence is "recorded as supporting evidence only"? [Completeness, Spec §FR-013] — ⚠ OPEN: FR-013 and FR-017 name "supporting_evidence" as optional but do not enumerate its sub-fields or shape — ✓✓ Data-Model §6 (closed five-field shape: `body_confidence_mean`, `body_confidence_min`, `body_line_count`, `body_token_count`, `header_band_excluded`); MI-15; Schema-Amendments §evaluation_document.schema.json
- [x] CHK009 Are the row-level reasons within the `semantic_table_quality` object (FR-017) defined with a concrete shape — e.g. a list of per-row objects, each with a `row_id` and a list of failed checks — or is "row-level reasons" an undefined term? [Completeness, Spec §FR-017] — ✓ FR-015 / FR-017: FR-015 defines the failed-check record shape (including `row_id`) and FR-017 lists `row_reasons` as a required field; together they give the per-row structure

---

## Attribute Clarity

- [x] CHK010 Is "expected/required row text (or required text fragments)" (FR-002, Key Entities) defined as a single string field or a list of strings, and what is the JSON key name? [Clarity, Spec §FR-002] — ⚠ OPEN: spec does not specify the JSON key name for the row-text field or whether it is a string vs list of strings — ✓✓ Data-Model §2 (field `required_row_text_tokens`: non-empty array of non-empty strings; JSON key name pinned); Schema-Amendments §semantic_table_truth.schema.json (`required_row_text_tokens`, `minItems: 1`, items `type: string, minLength: 1`)
- [x] CHK011 Is the distinction between "expected row text" and "required text fragments" made precise — i.e. is "required text fragments" a subtype of a list, or an alternative field, or are the terms used interchangeably? [Clarity, Spec §Clarifications Q2] — ⚠ OPEN: Q2 and FR-002 use both terms without formally distinguishing them or mapping to separate JSON keys — ✓✓ Data-Model §2 (single field `required_row_text_tokens`: a non-empty array of non-empty strings; "required text fragments" and "expected row text tokens" both map to this single array field — terms are unified under this JSON key)
- [x] CHK012 Is "quantity" in the sidecar row defined with a type and format (e.g. integer string, decimal string, or free string), or is only `unit_price`/`amount` given an explicit format? [Clarity, Spec §FR-002] — ⚠ OPEN: FR-002 gives explicit format only for `unit_price`/`amount`; `quantity` type/format is unspecified — ✓✓ Data-Model §2 field table (`quantity: string, optional; no format pattern`); Schema-Amendments §semantic_table_truth.schema.json (`quantity: {type: string}` — free string, no pattern constraint, intentionally; only `unit_price`/`amount` carry the decimal pattern)
- [x] CHK013 Is "description" in the sidecar row defined with a type constraint (e.g. non-empty string), or is any string value acceptable? [Clarity, Spec §FR-002] — ⚠ OPEN: FR-002 does not constrain `description` beyond being an optional field — ✓✓ Data-Model §2 field table (`description: string, optional`); Schema-Amendments §semantic_table_truth.schema.json (`description: {type: string}` — any string acceptable, no minLength, intentionally; description becomes a required-content check when present)
- [x] CHK014 Is "normalized decimal string without a currency symbol" (Clarifications Q9) defined with a canonical example and a formal grammar or regex, or only illustrated with `"21.00"`? [Clarity, Spec §Clarifications Q9] — ⚠ OPEN: Q9 gives only the example `"21.00"`; no formal regex or grammar for the sidecar decimal format is specified (the money regex in Q10/FR-010 applies to observed OCR, not sidecar values) — ✓✓ Data-Model §9 Regex constants (`EXPECTED_CELL_DECIMAL_REGEX = r"^\d+\.\d{2}$"` in `validator/semantic_table_truth.py`); Schema-Amendments §semantic_table_truth.schema.json (`unit_price`/`amount` have `"pattern": "^\\d+\\.\\d{2}$"`) — exactly two decimal places required
- [x] CHK015 Is the "observed body OCR evidence" entity definition precise about which sub-structure of `preprocess_output.json` constitutes the body/table region — e.g. specific JSON keys, a page index, or a region type label — rather than simply referencing the file? [Clarity, Spec §Key Entities] — ✓ FR-007 / Q22: Q22 and FR-007 define "observed body OCR evidence" as every OCR line and token on every page EXCEPT the feature-020 page-1 header-band region; lane-robust; no table-block detection dependency
- [x] CHK016 Is "row-level reasons" (FR-017) defined in a way that distinguishes per-row reasons from per-failed-check evidence, or are the two concepts conflated? [Clarity, Spec §FR-017] — ⚠ OPEN: FR-017 lists `row_reasons` as a required field but does not formally separate per-row aggregation from the per-check evidence records in FR-015; the relationship between `row_reasons` and `failed_checks` is prose-only — ✓✓ Data-Model §5 (Row Reason: per-row aggregation derived from `failed_checks`; fields `row_id`, `failed_categories`, `reason_text`; §4 Failed Check is the per-check source of truth); MI-14 (formal statement: `row_reasons` MUST be derivable by grouping `failed_checks` by `row_id`)
- [x] CHK017 Is "whole-invoice / table-quality claim" (FR-025, Clarifications Q12) defined as a precise field name and value in the data model, or only described in prose as a concept? [Clarity, Spec §FR-025] — ✓ FR-025 / Q12: defined as `document_pass_fail.semantic_table_quality_passed` with domain `true`/`false`/`null`
- [x] CHK018 Is "not passed (or requiring review)" (FR-025) a single closed-vocabulary value for `document_pass_fail.semantic_table_quality_passed` or two distinct values, and is the exact vocabulary specified? [Clarity, Spec §FR-025] — ✓ FR-025 / Q20: FR-025 and Q20 define the field as boolean-or-null: `true` (passed), `false` (failed or unevaluable), `null` (not_applicable) — three distinct values, not a string
- [x] CHK019 Is "unevaluable" (FR-016, Edge Cases) defined with a concrete cause vocabulary — e.g. a list of named error codes or cause strings — or only described as "sidecar present but inputs missing/unreadable"? [Clarity, Spec §Edge Cases] — ⚠ OPEN: spec describes "unevaluable" with a named cause in the edge-case prose but does not enumerate a closed cause-code vocabulary — ✓✓ Data-Model §7 (`cause` field: closed enum `preprocess_output_missing` | `preprocess_output_invalid_json` | `preprocess_output_schema_invalid` | `body_ocr_unreadable`); MI-13; Schema-Amendments §evaluation_document.schema.json (`cause` enum with these four values)
- [x] CHK020 Is "supporting evidence" (FR-013) defined as a named field or sub-object on the verdict or on each failed check, or is it an informal term without a data-model location? [Clarity, Spec §FR-013] — ⚠ OPEN: FR-013 and FR-017 reference `supporting_evidence` as an optional field but its shape and location (on the verdict vs on each check) are not specified — ✓✓ Data-Model §6 (Supporting Evidence: closed sub-object on `semantic_table_quality` verdict, NOT on individual failed checks; five named fields); MI-5 and MI-15 (location and shape enforcement); Schema-Amendments §evaluation_document.schema.json

---

## Identity & Uniqueness Rules

- [x] CHK021 Is the rule that `row_id` must be unique within the sidecar (Clarifications Q11) stated in a way that specifies the scope precisely — unique across all rows of the single sidecar file, not globally unique — and does the validator's acceptance predicate reflect that? [Clarity, Spec §Clarifications Q11] — ✓ FR-002 / Q11: FR-002 states "unique within the sidecar"; FR-004 requires the validator to check uniqueness within the sidecar as part of acceptance
- [ ] CHK022 Is "non-empty string" for `row_id` (Clarifications Q11) defined to exclude whitespace-only strings, or is a string of spaces considered non-empty by this spec? [Clarity, Spec §Clarifications Q11] — ⚠ OPEN: spec says "non-empty string" (FR-002/Q11) but does not address whitespace-only strings — STILL OPEN at impl-time: Schema-Amendments uses `minLength: 1` which permits whitespace-only strings; whether to add a `pattern` constraint excluding whitespace-only `row_id` is an implementation decision not resolved by current artifacts
- [ ] CHK023 Does the spec define what character set or length limit, if any, applies to `row_id`, or is any non-empty string unconditionally valid? [Completeness, Spec §FR-002] — ⚠ OPEN: no character-set or length constraint on `row_id` is specified — STILL OPEN at impl-time: Data-Model §2 and Schema-Amendments both use only `minLength: 1` with no character-set or max-length constraint; the decision to leave it fully open is implicit but not explicitly stated as a design choice
- [x] CHK024 Is the `document_id` matching rule (FR-003) defined precisely — e.g. exact string equality, case-sensitive, against the folder base name — so that the validator check is unambiguous? [Clarity, Spec §FR-003] — ✓ FR-003: FR-003 states the sidecar's `document_id` must match "the per-document folder it lives in"; US1 acceptance scenario 2 requires "naming the mismatch" — implies exact folder-basename string equality
- [x] CHK025 Is the `document_id` in the sidecar specified as a string type, and is its format defined (e.g. must it match the `inv_XXX_<difficulty>` pattern or is any string valid)? [Completeness, Spec §FR-003] — ⚠ OPEN: FR-003 requires `document_id` to match the folder name but does not specify its JSON type or format constraint separately — ✓✓ Data-Model §1 (`document_id: string, required, MUST match the containing per-document folder's basename exactly`); Schema-Amendments §semantic_table_truth.schema.json (`"document_id": {type: string, minLength: 1}`; folder-basename matching is enforced at validator layer not by JSON Schema — intentional design choice per MI-13 / Q41)

---

## Field Optionality & Defaults

- [x] CHK026 Is the optionality of `quantity`, `description`, `unit_price`, and `amount` in a sidecar row (Clarifications Q8) expressed using standard JSON Schema keywords (e.g. via the `required` array), or left as prose only? [Completeness, Spec §Clarifications Q8] — ⚠ OPEN: optionality is stated in prose only (FR-002/Q8); JSON Schema representation left for schema-authoring time — ✓✓ Schema-Amendments §semantic_table_truth.schema.json (row `required` array contains only `["row_id", "required_row_text_tokens"]`; `quantity`, `description`, `unit_price`, `amount` are defined as properties but absent from `required` — JSON Schema keyword correctly encodes optionality)
- [x] CHK027 Does the spec state what value is used for fields omitted from a sidecar row at evaluation time — e.g. are they skipped, treated as "not checked", or explicitly represented as `null`? [Completeness, Spec §FR-002] — ✓ FR-002 / Q8: FR-002 states "every one of those fields that IS present becomes a required-content check for that row" — omitted fields are skipped (not checked), which answers the evaluation-time behavior
- [x] CHK028 Is the behavior of the gate when a row is present in the sidecar but every optional field is absent (only `row_id` and row text are present) fully specified — does the gate run only the text-coverage and alignment checks, or does it also require at least one value check? [Completeness, Spec §Clarifications Q8] — ✓ FR-002 / Q8: only present optional fields generate required-content checks; a row with only `row_id` + row text runs text-coverage and alignment checks only
- [x] CHK029 Is the optionality of the `semantic_table_quality` object in `evaluation_document.json` (FR-017) defined with a schema keyword (e.g. `"required": false`), or left implicit? [Completeness, Spec §FR-017] — ⚠ OPEN: FR-017 says the object "MAY be omitted" when no sidecar; optionality is prose-only, not JSON Schema keyword — ✓✓ Schema-Amendments §evaluation_document.schema.json (`semantic_table_quality` is listed as a property but NOT in the `required` array of the top-level schema — JSON Schema keyword correctly encodes optionality; `document_pass_fail.semantic_table_quality_passed` also optional in schema for backward-compat)
- [x] CHK030 Is the default or absent representation of `document_pass_fail.semantic_table_quality_passed` for pre-feature reports (FR-019) specified — e.g. field absent means "gate did not run", or field is `null`, or field is `"not applicable"`? [Completeness, Spec §FR-019] — ⚠ OPEN: FR-019 requires pre-feature reports to remain valid but does not specify how a reader interprets an absent `semantic_table_quality_passed` field (the edge case "gate did not run" interpretation is mentioned in prose but not formalized) — ✓✓ MI-24 (formal invariant: absent field MUST be interpreted as `null` semantically — "gate did not run"); Research R-022.9; Evaluator-Output-Contract §Backward-compat readers (explicit prose contract for reader interpretation)
- [x] CHK031 Is the optionality of approximate row-region anchors (mentioned as "optional future metadata" in FR-002 and Key Entities) expressed in the JSON Schema or contract, so the validator correctly ignores extra fields rather than rejecting them? [Completeness, Spec §FR-002] — ⚠ OPEN: FR-002 notes anchors are "optional future metadata" and MUST NOT be required; whether the schema uses `additionalProperties: true` or explicit optional fields is left for schema-authoring — ✓✓ Schema-Amendments §semantic_table_truth.schema.json (row uses `additionalProperties: false`; anchors are NOT listed as properties, meaning they would currently be rejected — this is a conscious design decision: anchors are deferred to a future schema amendment when actually specified; no partial fields accepted)

---

## Value Representation

- [x] CHK032 Is the canonical money regex `^\$?\d{1,3}(,\d{3})*\.\d{2}$` (Clarifications Q10, FR-010) unambiguous about whether it is applied to the raw observed OCR token or to the normalized (post-NFKC, post-whitespace-collapse) token? [Clarity, Spec §FR-010] — ✓ FR-010 / Q16: FR-010 explicitly states "raw observed OCR token text before FR-009 punctuation stripping or other comparison normalization"; Q16 confirms currency-shape checks run before normalization
- [x] CHK033 Does the spec address whether the canonical money regex permits a leading `$` on the observed OCR side while requiring the sidecar to store the value *without* a currency symbol, and whether this asymmetry is intentional and fully specified? [Consistency, Spec §FR-002 / Spec §FR-010] — ✓ FR-002 / FR-010 / Q9: FR-002 and Q9 require sidecar values as decimal strings without `$`; FR-010 regex applies to raw OCR tokens and allows optional `$` — the asymmetry is intentional and fully described
- [x] CHK034 Is "normalized decimal string without a currency symbol" (e.g. `"21.00"`) required to carry exactly two decimal places, or could `"21.5"` or `"21.000"` be valid sidecar values? [Clarity, Spec §Clarifications Q9] — ⚠ OPEN: Q9 illustrates with `"21.00"` but does not specify the required number of decimal places for sidecar `unit_price`/`amount` — ✓✓ Data-Model §9 (`EXPECTED_CELL_DECIMAL_REGEX = r"^\d+\.\d{2}$"` — exactly two decimal places required); Schema-Amendments §semantic_table_truth.schema.json (`pattern: "^\\d+\\.\\d{2}$"` — rejects `"21.5"` and `"21.000"`)
- [x] CHK035 Is "exact token or substring containment" (FR-009) defined to specify whether containment means the sidecar value is a substring of the OCR text, the OCR text is a substring of the sidecar value, or either direction qualifies? [Clarity, Spec §FR-009] — ✓ FR-009: FR-009 states "the full normalized sidecar value appears as a contiguous substring in normalized observed body OCR" — direction is unambiguous (sidecar value ⊆ OCR text)
- [x] CHK036 Is the normalization pipeline — "Unicode NFKC normalization, case-folding, whitespace collapse, and punctuation stripping" (FR-009) — defined with an application order that makes the result deterministic (e.g. NFKC first, then case-fold, then whitespace-collapse, then punctuation-strip)? [Clarity, Spec §FR-009] — ✓ FR-009 / Q5: FR-009 states the operations as "Unicode NFKC normalization, Python `str.casefold()` case-folding, collapse of all Unicode whitespace to a single ASCII space, and stripping of Unicode punctuation after NFKC" — the listed order is the application order
- [x] CHK037 Is "punctuation stripping" in the normalization pipeline (FR-009) defined precisely — e.g. which Unicode categories are stripped — or is it an informal term that leaves implementation-specific variance? [Clarity, Spec §Clarifications Q5] — ⚠ OPEN: FR-009 says "stripping of Unicode punctuation after NFKC" but does not name the Unicode categories (e.g. P* categories) or give a regex, leaving variance — ✓✓ Data-Model §11 (normalization pipeline code listing: step 4 uses `unicodedata.category(ch).startswith("P")` covering all seven P* subcategories: Pc/Pd/Pe/Pf/Pi/Po/Ps); Research R-022.2 (decision + rationale)
- [x] CHK038 Is the closed set of four failed-check category labels {missing-required-content, malformed-currency-shape, row-text-coverage-gap, row-alignment-failure} (FR-016, Key Entities) specified with exact string values for use in JSON output, or only named in prose? [Clarity, Spec §FR-016] — ✓ FR-015 / Q17 / Q18 / Q26: FR-015 and Q17/Q18 name the four categories in kebab-case as exact JSON strings; Q26 confirms category labels remain kebab-case
- [x] CHK039 Is the closed status set {passed, failed, not applicable, unevaluable} (FR-016, Key Entities) specified with exact JSON string values (e.g. is "not applicable" stored as `"not applicable"` with a space or `"not_applicable"` with an underscore)? [Clarity, Spec §FR-016] — ✓ FR-016 / Q26: Q26 defines the four canonical snake_case values: `passed`, `failed`, `not_applicable`, `unevaluable` — used verbatim in JSON output

---

## Entity Relationships

- [x] CHK040 Is the relationship between the Semantic Quality Verdict and the set of Failed Checks — i.e. whether the verdict entity embeds the checks, references them, or they are separate top-level arrays — specified in the data model? [Completeness, Spec §Key Entities] — ⚠ OPEN: FR-017 lists `failed_checks` as a field of the `semantic_table_quality` object but the nesting relationship between the verdict entity and the failed-check array is not formally specified in the data model — ✓✓ Data-Model §7 (Semantic Quality Verdict field table: `failed_checks` is embedded as an array field directly on the `semantic_table_quality` object — it is NOT a separate top-level array); Schema-Amendments §evaluation_document.schema.json (verbatim JSON Schema shows `failed_checks` nested inside `semantic_table_quality`)
- [x] CHK041 Is the relationship between `document_pass_fail.semantic_table_quality_passed` and the Semantic Quality Verdict status explicitly specified — e.g. "failed" verdict → `semantic_table_quality_passed` is set to a specific value — or is it described only in prose? [Completeness, Spec §FR-025] — ✓ FR-025 / Q20: FR-025 and Q20 explicitly map each status value to a boolean-or-null: `passed`→`true`, `failed`→`false`, `unevaluable`→`false`, `not_applicable`→`null`
- [x] CHK042 Is the relationship between the sidecar `document_id` and the per-document folder name fully specified — e.g. must the `document_id` be exactly the folder's `basename`, or can it be a path-relative or qualified form? [Clarity, Spec §FR-003] — ⚠ OPEN: FR-003 says `document_id` must match "the per-document folder it lives in" but does not specify basename vs. path-relative vs. qualified form — ✓✓ Data-Model §1 (Validator error requirements: error MUST name "the containing folder basename"; implies basename comparison); Validator-CLI-Contract §validate folder (step 4: "Compares `document_id` against the folder basename — the last path component of `<folder-path>`" — basename is the exact rule)
- [ ] CHK043 Is the relationship between the Observed Body OCR Evidence entity and the existing `preprocess_output.json` schema specified precisely enough to identify which JSON key paths in `preprocess_output.json` the gate reads, rather than referring to the artifact as a whole? [Completeness, Spec §Key Entities] — ⚠ OPEN: Q22 and FR-007 define the exclusion rule (all pages minus page-1 header band) but do not specify the JSON key paths within `preprocess_output.json` (e.g. `pages[*].blocks[*].lines[*]` or similar) — STILL OPEN at impl-time: Plan §Source-Code describes `semantic_quality_body_ocr.py` as traversing `pages[*].blocks[*].lines` / `pages[*].lines` but the exact path depends on whether the preprocessing was OCR-only or PPStructureV3 — the exact traversal logic is a code-time decision
- [x] CHK044 Does the spec specify whether the `semantic_table_quality` object in `evaluation_document.json` (FR-017) shares its schema with any part of the Semantic Quality Verdict entity, or whether they are independently defined? [Consistency, Spec §FR-017 / Spec §Key Entities] — ⚠ OPEN: spec does not state whether the report field is the serialized verdict object itself or a separately defined shape — ✓✓ Data-Model §7 (Semantic Quality Verdict IS the `semantic_table_quality` object — the same field table defines both; the verdict entity is the serialized form of the report field, not a separately defined shape); Schema-Amendments §evaluation_document.schema.json (verbatim JSON Schema for `semantic_table_quality` matches Data-Model §7)
- [x] CHK045 Is the relationship between per-document semantic status in `evaluation_run_summary.json` (FR-018) and the per-document `semantic_table_quality` object in `evaluation_document.json` (FR-017) specified — i.e. is run-summary status derived from the document-level verdict, and how? [Completeness, Spec §FR-018] — ✓ FR-018 / Q19: FR-018 specifies that per-document status entries carry `semantic_table_quality_status` derived from the verdict status; the run-summary aggregates over per-document verdicts

---

## Status/Verdict Vocabulary

- [x] CHK046 Is "not applicable" (FR-016, FR-025) specified as a single vocabulary term covering both (a) no sidecar present and (b) no semantic truth authored, or are these treated as distinct sub-states? [Clarity, Spec §FR-016] — ✓ FR-016 / Q26 / FR-005: `not_applicable` is defined as the single status for any document without a sidecar; FR-005 confirms the no-sidecar case continues under existing contract — single term covers both formulations
- [x] CHK047 Is "unevaluable" (FR-016, Edge Cases) distinct from "failed" in the JSON output, and does the spec define whether `document_pass_fail.semantic_table_quality_passed` carries a specific value when the verdict is "unevaluable"? [Completeness, Spec §FR-016 / Spec §FR-025] — ✓ FR-016 / FR-025 / Q20: `unevaluable` is a separate status string; Q20 and FR-025 both specify `document_pass_fail.semantic_table_quality_passed` is `false` for `unevaluable`
- [x] CHK048 Does the spec define what value `document_pass_fail.semantic_table_quality_passed` carries when the verdict is "passed" — e.g. `true`, `"passed"`, or another closed-vocabulary string? [Completeness, Spec §FR-025] — ✓ FR-025 / Q20: explicitly `true` (boolean) when status is `passed`
- [x] CHK049 Is the aggregation rule "any-fail" (Clarifications Q4, FR-016) expressed in a way that is testable as a predicate — i.e. "failed if `len(failed_checks) > 0`" — or only described in natural language that allows boundary ambiguity? [Measurability, Spec §FR-016] — ✓ FR-016 / Q4: FR-016 states "failed if any row records any failed check" and "passed only when zero failed checks are recorded across all rows" — directly maps to `len(failed_checks) > 0`
- [x] CHK050 Does the spec define whether the any-fail aggregation considers only top-level failed checks or also failed checks nested under row-level reasons, and whether the result is the same regardless of nesting? [Clarity, Spec §Clarifications Q4] — ⚠ OPEN: FR-016 says "any row records any failed check" but the structural relationship between `failed_checks` (top-level array) and `row_reasons` is unspecified; nesting ambiguity remains — ✓✓ MI-10 (any-fail rule operates on `len(failed_checks) > 0` — explicitly the flat array, not `row_reasons`); MI-14 (`row_reasons` is derived, not the source of truth — aggregation uses `failed_checks` exclusively)
- [x] CHK051 Is the verdict status produced when the sidecar is present but `preprocess_output.json` is missing — "unevaluable" per the edge case — distinguished from the case where `preprocess_output.json` exists but the body OCR section is empty ("failed"), so the two produce different status values? [Clarity, Spec §Edge Cases] — ✓ Edge Cases: the two edge cases are explicitly distinguished — missing/unreadable `preprocess_output.json` → `unevaluable`; empty body OCR → `failed` (all required-content checks fail)

---

## Data Volume & Scale Assumptions

- [ ] CHK052 Does the spec state any upper bound on the number of rows a `semantic_table_truth.json` sidecar may declare, or is any number of rows acceptable? [Completeness, Gap] — ⚠ OPEN: no row-count upper bound specified — STILL OPEN at impl-time: neither data-model.md, schema-amendments.md, nor module-invariants.md define an upper bound; any positive count is accepted; this is an explicit non-decision (no performance gate exists per Q38)
- [ ] CHK053 Does the spec state any upper bound on the number of required text fragments per row, or is any list length acceptable? [Completeness, Gap] — ⚠ OPEN: no upper bound on required text fragments per row specified — STILL OPEN at impl-time: Data-Model §2 and schema use only `minItems: 1`; no maxItems; any positive list length is acceptable; same rationale as CHK052 (no performance gate)
- [x] CHK054 Is the minimum number of rows in a valid sidecar (the zero-row rejection edge case) expressed as a formal constraint in the contract, or only mentioned as an edge case in prose? [Completeness, Spec §Edge Cases] — ⚠ OPEN: zero-row rejection is stated as an edge case in prose only; no formal `"minItems": 1` or equivalent specified — ✓✓ Schema-Amendments §semantic_table_truth.schema.json (`"rows": {type: array, minItems: 1}` — formally encoded in JSON Schema); Data-Model §1 (`rows`: "Non-empty")
- [x] CHK055 Does the spec state whether the data-model entities are sized to handle multi-page invoices — e.g. whether "observed body OCR evidence" covers only page 1 or all pages — or is the page scope left unspecified? [Completeness, Gap] — ✓ FR-007 / Q22: Q22 explicitly states "every OCR line and token on every page … EXCEPT the feature-020 page-1 header-band region" — multi-page invoices are covered

---

## Ambiguities & Conflicts

- [x] CHK056 Is there a potential conflict between FR-002 requiring `unit_price`/`amount` as "normalized decimal strings without a currency symbol" and FR-010 defining the currency-shape check with the regex `^\$?\d{1,3}(,\d{3})*\.\d{2}$`, which permits a leading `$`? Does the spec explicitly reconcile this asymmetry so it cannot be misread as requiring the sidecar to store `$`-prefixed values? [Conflict, Spec §FR-002 / Spec §FR-010] — ✓ FR-002 / FR-010 / Q9 / Q16: spec explicitly separates sidecar storage (no `$`, normalized decimal string) from the OCR-side regex (optional `$` on raw observed token); FR-010 clarifies the regex applies to observed OCR, not sidecar — asymmetry is intentional and reconciled
- [x] CHK057 Is there a potential ambiguity between the binary coverage rule in FR-011 ("any required/expected text token absent … is a row-text-coverage gap") and the row-alignment failure in FR-012 ("row's values are missing, shifted, or split")? Does the spec define whether "missing" in FR-012 can also trigger a row-text-coverage-gap check in FR-011, making the same absence produce two distinct failed-check records? [Ambiguity, Spec §FR-011 / Spec §FR-012] — ✓ FR-018 / Q18: Q18 explicitly defines each check category by predicate and states a row may record more than one failed category when multiple predicates are true — co-occurrence of row-text-coverage-gap and row-alignment-failure for the same absence is intentional
- [x] CHK058 Is the coverage ratio ("a coverage ratio MAY be recorded as supporting evidence but MUST NOT be the trigger", FR-011) defined with enough specificity to prevent an implementer from defining a proprietary ratio formula that differs between runs, breaking SC-007 reproducibility? [Ambiguity, Spec §FR-011] — ⚠ OPEN: FR-011 says coverage ratio MAY be recorded as supporting evidence but does not specify the formula; a proprietary implementation could break SC-007 reproducibility — ✓✓ Data-Model §6 (`supporting_evidence` has a closed five-field shape with `additionalProperties: false`; no coverage ratio field is in the closed shape, so no proprietary ratio can enter the serialized output; reproducibility risk is eliminated by closure, not by specifying the formula)
- [x] CHK059 Does the spec reconcile the phrase "required values plus row text" (Clarifications Q2) with the statement that `quantity`, `description`, `unit_price`, and `amount` are "optional" (Clarifications Q8) — i.e. is "required values" in Q2 referring to the values of whichever fields are authored rather than to those four fields being universally required? [Conflict, Spec §Clarifications Q2 / Spec §Clarifications Q8] — ✓ FR-002 / Q8: FR-002 explicitly states "only `row_id` and the expected/required row text are mandatory in every row; `quantity`, `description`, `unit_price`, and `amount` are optional, and every one of those fields that IS present becomes a required-content check" — reconciles Q2 intent
- [x] CHK060 Is the Key Entities definition of Semantic Quality Verdict consistent with the FR-016 definition — specifically, does the Key Entities description include "row-level reasons" while FR-016 does not list them as part of the verdict entity, and is this gap intentional? [Consistency, Spec §FR-016 / Spec §Key Entities] — ✓ FR-016 / FR-017 / Key Entities: Key Entities describes the verdict as including "failed checks, row-level reasons, and OCR confidence as supporting evidence"; FR-017 places `row_reasons` in the `semantic_table_quality` report object; the two are consistent — row-level reasons flow from verdict to report
- [x] CHK061 Does the spec define whether `document_pass_fail.semantic_table_quality_passed` is a boolean or a string, given that FR-025 uses the phrase "not passed (or requiring review)" which implies a non-boolean vocabulary? [Ambiguity, Spec §FR-025] — ✓ FR-025 / Q20: Q20 explicitly resolves this — the field is boolean-or-null (`true`, `false`, or `null`); the phrase "not passed" maps to `false`
- [x] CHK062 Is there a potential conflict between FR-019 (evaluation report schemas must remain tolerant of pre-feature reports) and FR-031 (contract-set version advances to 1.3.0) — specifically, does the spec define whether the new 1.3.0 contract validates existing 1.2.x evaluation reports without error? [Conflict, Spec §FR-019 / Spec §FR-031] — ⚠ OPEN: FR-019 requires tolerance of pre-feature reports but does not specify that the v1.3.0 contract schema must accept v1.2.x artifacts; the validation predicate is not defined in the spec — ✓✓ Schema-Amendments §evaluation_document.schema.json (both new fields are optional — not in the top-level `required` array — so pre-v1.3.0 reports remain valid); Schema-Amendments §evaluation_run_summary (both `semantic_table_quality_metrics` and `semantic_document_statuses` are optional); Research R-022.9; MI-24

---

## Notes

- This checklist gates whether the data-model requirements are precise enough to write a conforming JSON Schema and an unambiguous implementation — field names, types, optionality, value vocabularies, and entity relationships must all be resolved before schema authoring begins.
- Special attention is warranted on the sidecar envelope shape (CHK001–CHK003), the closed status/verdict vocabulary with exact JSON string values (CHK038–CHK039, CHK048), and the normalization pipeline ordering (CHK036–CHK037), which are underspecified in the current spec text.
- The asymmetry between sidecar currency storage (no `$`) and the observable money regex (optional `$`) identified in CHK056 is the highest-risk conflict for the currency-shape check implementation.

---

## Verification — 2026-05-22 (re-verified against spec.md with Clarifications Q1-Q26)

- Items resolved: 27 / 62
- Items still open: 35
- Open items: CHK001 (sidecar envelope top-level fields beyond `document_id`+rows not enumerated), CHK002 (JSON key name for rows array not specified), CHK003 (any positive row count implied but not stated), CHK008 (supporting_evidence sub-field shape unspecified), CHK010 (row-text field JSON key name and string-vs-list not specified), CHK011 (expected-row-text vs required-text-fragments distinction not formalized), CHK012 (`quantity` type/format unspecified), CHK013 (`description` type constraint unspecified), CHK014 (sidecar decimal format — regex or grammar not given), CHK016 (`row_reasons` vs `failed_checks` structural relationship prose-only), CHK019 (unevaluable cause-code vocabulary not enumerated), CHK020 (supporting_evidence shape and location unspecified), CHK022 (whitespace-only `row_id` not addressed), CHK023 (`row_id` character-set/length not specified), CHK025 (`document_id` JSON type and format constraint not specified), CHK026 (optionality expressed in prose only, not JSON Schema keywords), CHK029 (optionality of `semantic_table_quality` object — JSON Schema keyword not specified), CHK030 (absent `semantic_table_quality_passed` in pre-feature reports — consumer interpretation not formalized), CHK031 (future-anchor `additionalProperties` handling not specified), CHK034 (decimal places required for sidecar values not specified), CHK037 (Unicode punctuation categories for stripping not specified), CHK040 (verdict-entity vs failed-checks nesting relationship not formally specified), CHK042 (`document_id` basename vs. path-relative form not specified), CHK043 (JSON key paths in `preprocess_output.json` not specified), CHK044 (`semantic_table_quality` report field vs. verdict entity — shared vs. independent schema not stated), CHK050 (any-fail aggregation nesting ambiguity — `failed_checks` vs `row_reasons` not resolved), CHK052 (no row-count upper bound), CHK053 (no text-fragment upper bound per row), CHK054 (zero-row rejection formal constraint missing), CHK058 (coverage ratio formula not specified — SC-007 reproducibility risk), CHK062 (v1.3.0 schema acceptance of v1.2.x artifacts not specified)

---

## Verification — 2026-05-23 (re-verified post /speckit.plan)

- Items resolved this round: 26
- Items still open at impl-time: 5
- Open items: CHK022 (whitespace-only `row_id` — `minLength: 1` permits spaces; impl must decide whether to add a pattern constraint), CHK023 (`row_id` character-set/length — explicitly unconstrained beyond `minLength: 1`; non-decision left to impl), CHK043 (JSON key paths in `preprocess_output.json` — lane-dependent traversal path is a code-time decision), CHK052 (no row-count upper bound — explicit non-decision; any positive count accepted), CHK053 (no text-fragment upper bound per row — same non-decision)
- Cumulative resolved: 57 / 62  (Round 1: 31, Round 2: 26)
