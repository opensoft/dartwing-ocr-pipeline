# Security & Data-Protection Requirements Quality Checklist: OCR Semantic Quality Gate

**Purpose**: Validate that security, data-protection, PII-screening, network-isolation, and fixture-provenance requirements are complete, clear, consistent, and measurable before implementation begins.
**Created**: 2026-05-22
**Feature**: [spec.md](../spec.md)

---

## Data Protection & PII Requirements

- [x] CHK001 Is a PII/license pre-inclusion screening requirement stated for any degraded-body fixture before it is placed in `tests/stage1_semantic_quality/`, given that invoice fixtures carry vendor names, amounts, and tax-id-shaped tokens — or is this screening left implicit by reference to the labeling guide? [Completeness, Gap] — ⚠ OPEN: no explicit PII screening requirement for the new corpus root; FR-026 requires labeling-guide amendments only for promotion of noncanonical folders, not for the new root in general — ✓✓ Q44 + FR-026: "The labeling-guide.md PII / license pre-inclusion screening MUST apply identically to fixtures under tests/stage1_semantic_quality/ and tests/stage1_vendor_identity/; every future committed fixture under tests/stage1_semantic_quality/ MUST pass the same screening." The requirement is now explicit and normative.
- [x] CHK002 Does the spec reference the project's labeling-guide PII/license pre-inclusion screening checklist as an explicit requirement that must be satisfied before a `semantic_table_truth.json` sidecar (and its associated source PDF) is committed to the repo? [Completeness, Spec §US5] — ⚠ OPEN: FR-026 mentions "labeling-guide amendments" as part of the promotion bundle, but does not cite the labeling-guide's PII checklist as an explicit pre-commit gate for sidecars — ✓✓ Q44 + FR-026: labeling-guide screening is now cited as a hard MUST gate for every future committed fixture under tests/stage1_semantic_quality/ — including sidecars; the explicit reference is in FR-026's Q44 clause.
- [x] CHK003 Is there a stated requirement that the `semantic_table_truth.json` sidecar itself (not just the source PDF) undergoes PII screening before promotion, given that authored row truth records exact vendor amounts, description text, and other invoice content? [Completeness, Gap] — ⚠ OPEN: no requirement for PII screening of the sidecar as a distinct artifact — ✓✓ Q44 + FR-026: "every future committed fixture under tests/stage1_semantic_quality/ MUST pass the same screening" — a fixture includes both the sidecar and source PDF; the labeling-guide screening applies to the committed fixture as a unit including the sidecar.
- [x] CHK004 Is the sensitivity level of `semantic_table_truth.json` — specifically whether it is treated as a data artifact subject to the same PII/license discipline as `expected.json` — explicitly classified in the spec or referenced governance doc? [Clarity, Gap] — ⚠ OPEN: sensitivity classification not stated anywhere in the spec — STILL OPEN at impl-time: no planning artifact provides this classification; a policy/governance decision outside feature 022 scope — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.
- [x] CHK005 Is a requirement stated for scrubbing or anonymizing real invoice data (vendor names, tax-id-shaped tokens, monetary amounts) before it enters authored row truth in `semantic_table_truth.json`? [Completeness, Gap] — ⚠ OPEN: no scrubbing or anonymization requirement stated; Q25 uses a synthetic fixture for US2 but does not generalize to all sidecars — STILL OPEN at impl-time: scrubbing/anonymization policy is a data-governance decision not addressed by any planning artifact; deferred to corpus promotion change — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.
- [x] CHK006 Does the spec define who is authorized to promote a noncanonical fixture (e.g. `inv_024_hard_degraded_body`) to a committed scored corpus sample, or does it leave access control implicit? [Completeness, Spec §FR-026] — ⚠ OPEN: FR-026 defines what amendments must accompany a promotion change but does not name who is authorized to approve or execute it — STILL OPEN at impl-time: authorization/access control is a governance process decision; not addressed in any planning artifact; deferred to corpus promotion change — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.
- [x] CHK007 Is there a stated requirement that `row_id` values in `semantic_table_truth.json` — which appear in failed-check records and evaluation reports — must not encode personally identifiable information? [Completeness, Gap] — ⚠ OPEN: FR-002 / FR-004 constrain row_id to non-empty string, unique within sidecar; no PII prohibition stated — STILL OPEN at impl-time: PII prohibition on row_id not in any planning artifact; impl should note in docs that row_id must follow labeling-guide naming conventions — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-2/B): `row_id` now matches the safety pattern `^[A-Za-z0-9_-]{1,64}$` (spec.md FR-002 / data-model.md §2 / contracts/schema-amendments.md). The pattern rejects path-traversal sequences, control characters, whitespace, punctuation-heavy PII-style labels, and runaway lengths.
- [x] CHK008 Are the data-retention and deletion requirements for noncanonical calibration samples (e.g. those in `/tmp/` benchmark paths from feature 021) stated relative to this feature's fixture lifecycle? [Completeness, Gap] — ⚠ OPEN: no data-retention or deletion requirement stated for calibration samples or benchmark scratch paths — STILL OPEN at impl-time: data-retention policy is a governance decision outside feature 022 scope; not addressed in any planning artifact — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.
- [x] CHK009 Is there a requirement specifying whether `evaluation_document.json` — which will carry row-level failed-check evidence including extracted OCR text — is treated as a sensitive artifact subject to PII controls? [Completeness, Gap] — ⚠ OPEN: evaluation_document.json PII sensitivity not classified in the spec — STILL OPEN at impl-time: sensitivity classification is a data-governance policy decision; not addressed in any planning artifact for this feature — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.
- [x] CHK010 Does the spec state any requirement about log-level output — e.g. whether the gate's "concrete failed check" records, which may contain invoice row text, are redacted at non-DEBUG log levels consistent with the labeling-guide PII discipline? [Completeness, Gap] — ⚠ OPEN: no log-level redaction requirement stated — STILL OPEN at impl-time: log-level redaction policy not addressed in any planning artifact; impl must decide whether to emit raw OCR text to DEBUG logs — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.

---

## Network & Isolation Requirements

- [x] CHK011 Is the prohibition on network I/O stated as an absolute, non-negotiable, runtime-enforced constraint — or only as a design intent — for the semantic gate? [Clarity, Spec §FR-008] — ✓ FR-008: "the semantic gate MUST NOT perform network I/O" uses the normative RFC-2119 MUST NOT; FR-029 adds a separate absolute prohibition on network calls
- [x] CHK012 Is the prohibition in FR-008 scoped to "the semantic gate module" or does it extend to the entire evaluator execution context when the gate runs? [Clarity, Spec §FR-008] — ⚠ OPEN: FR-008 scopes the prohibition to "the semantic gate"; whether the wider evaluator context inherits the prohibition when the gate runs is not stated — STILL OPEN at impl-time: scope boundary not clarified by any planning artifact; MI-1 scopes the prohibition to the gate module only — ✓✓✓ Closed 2026-05-23 (security-clarify auto-close Bucket C): MI-1 scopes the prohibition to the gate module only — the resolution is explicit in the invariant. No impl action required.
- [x] CHK013 Does FR-008 specify what the gate MUST do when a network call is attempted (e.g. raise an exception, hard-fail, return "unevaluable") rather than only stating the call must not be made? [Clarity, Spec §FR-008] — ⚠ OPEN: FR-008 prohibits the call but does not specify the gate's behavior if the prohibition is violated — STILL OPEN at impl-time: no planning artifact specifies the violation behavior; MI-19 (SemanticGateInvariantError) applies to impl bugs but not to external network-call attempts — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-5/A): no always-on runtime monkey-patch or import sentinel. MI-1 is a code invariant; T031 is the enforcement mechanism at CI/test time. Runtime defensive checks are intentionally omitted to avoid side effects on normal evaluator execution.
- [x] CHK014 Is there a stated requirement that the gate's isolation from network I/O is verifiable by a test (e.g. a network-blocking test mode or a network-call audit in the CI environment) — not merely asserted in prose? [Measurability, Spec §FR-008] — ⚠ OPEN: SC-007 states "no network access" as a reproduction criterion but does not require a network-blocking test — STILL OPEN at impl-time: R-022.7 specifies CPU-only pytest tests but no network-blocking test mechanism; impl must decide whether to add socket-mock or import-audit tests — ✓✓✓ Closed 2026-05-23 (security-clarify auto-close Bucket A): tasks.md T031 adds the socket-mock + Paddle-import-audit test — that IS the network-blocking test mechanism this item asked for.
- [x] CHK015 Is the prohibition on remote services (FR-029) and the prohibition on network I/O (FR-008) written as separate, independently testable constraints, or do they overlap without a clear boundary? [Clarity, Spec §FR-008, §FR-029] — ⚠ OPEN: the two prohibitions overlap (all network calls are remote-service calls in this context) and share no stated boundary; independently testable coverage not addressed — STILL OPEN at impl-time: the overlap is a spec quality issue; MI-1 covers both under a single invariant; impl should not treat them as requiring separate test coverage — ✓✓✓ Closed 2026-05-23 (security-clarify auto-close Bucket C): MI-1 covers both FR-008 and FR-029 under one invariant; treating them as a single testable constraint is correct. T031 is the unified enforcement test.
- [x] CHK016 Does FR-029 define what constitutes a "remote service" — specifically whether localhost Ollama (used by feature 005) is excluded from this prohibition in the context of the semantic gate? [Clarity, Spec §FR-029] — ⚠ OPEN: "remote service" is not defined; localhost Ollama exclusion is not addressed for this gate — STILL OPEN at impl-time: MI-1 says "no network I/O" unconditionally; impl should treat localhost Ollama as out of scope for this gate entirely — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-6/A): all network I/O is forbidden for the semantic gate, including localhost, loopback, Unix sockets used as network transports, Ollama, and any remote endpoint. MI-1's "no network I/O" is the unconditional rule; localhost/remote distinction does not apply.
- [x] CHK017 Is the "no network call" invariant stated as a success criterion in SC-007, and is the traceability from SC-007 back to FR-008 explicit? [Consistency, Spec §SC-007, §FR-008] — ⚠ OPEN: SC-007 contains "no network access" and FR-008 contains the prohibition, but no explicit "(FR-008)" cross-reference is made in SC-007 — STILL OPEN at impl-time: cross-reference omission is a spec quality issue; both MI-1 and module-invariants.md cover this — no impl action required — ✓✓✓ Closed 2026-05-23 (security-clarify auto-close Bucket C): spec-quality cross-reference omission; both SC-007 and FR-008 are covered by MI-1 in code. No impl action required.
- [x] CHK018 Does the spec state whether the gate is required to operate in an air-gapped or offline environment (matching the CPU-safe design intent), or is offline-capability left implicit? [Completeness, Spec §FR-008] — ⚠ OPEN: offline/air-gapped operation not stated explicitly; FR-008 + FR-029 together imply it but do not assert it as a named requirement — STILL OPEN at impl-time: offline/air-gap requirement not stated; MI-1 + MI-25 together imply CPU-safe offline operation; impl should treat this as implied by MI-1 — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-7/B): air-gapped operation is now a named functional requirement — **FR-034**: "The semantic gate MUST execute correctly in an air-gapped environment with no network access of any kind." (added to spec.md). Enforcement test: T031 (no new test added per Q-SEC-7 answer).

---

## Fixture Provenance & Licensing

- [x] CHK019 Is there a stated requirement that the source PDF for any fixture entering `tests/stage1_semantic_quality/` carries a verified license permitting its use as a test fixture — not only for the noncanonical calibration sample `inv_024_hard_degraded_body`, but for all future degraded-body additions? [Completeness, Spec §FR-026, §US5] — ⚠ OPEN: FR-026 requires labeling-guide amendments for promotion of noncanonical folders; the US2 synthetic fixture has no source.pdf (Q25); a general license requirement for source PDFs in tests/stage1_semantic_quality/ is not stated — ✓✓ Q44 + FR-026: labeling-guide PII/license pre-inclusion screening applies to all fixtures under tests/stage1_semantic_quality/; license verification is part of the labeling-guide screening gate for any future fixture with a source PDF.
- [x] CHK020 Does the spec or its referenced governance specify where license provenance evidence (e.g. a `notes.md` entry per the labeling guide) must be recorded for each fixture in `tests/stage1_semantic_quality/`? [Completeness, Spec §FR-027, §US5] — ⚠ OPEN: notes.md or equivalent provenance record not required for tests/stage1_semantic_quality/; FR-026 says the new root "MAY reuse" the existing per-document folder discipline — STILL OPEN at impl-time: provenance-record location not specified; deferred to corpus promotion change per FR-026 — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.
- [x] CHK021 Is the license/provenance screening step described as a gate that MUST be passed before a fixture's `semantic_table_truth.json` is committed, or only as a recommended practice? [Clarity, Spec §FR-026] — ✓ FR-026 / Q23: FR-026 uses "MUST NOT be promoted… unless the same feature change also delivers the… labeling-guide… amendments", making labeling-guide compliance a hard MUST gate for any real fixture promotion
- [x] CHK022 Is it specified whether the `inv_024_hard_degraded_body` calibration sample's source PDF provenance and license have already been screened at the time this feature is implemented, or is that screening deferred? [Completeness, Spec §US5, §Assumptions] — ⚠ OPEN: Assumptions state these remain calibration evidence only and are not promoted in this feature; provenance screening status is not stated — STILL OPEN at impl-time: provenance screening status of calibration samples is a governance decision; not a code decision for this feature — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.
- [x] CHK023 Does the corpus-promotion requirement (FR-026) specify that the labeling-guide PII/license checklist is a prerequisite gate in the same PR/change that promotes a degraded-body folder, or may it be deferred to a follow-up? [Clarity, Spec §FR-026] — ✓ FR-026: "A noncanonical folder MUST NOT be promoted… unless the same feature change also delivers the dataset, labeling-guide, folder-contract, and evaluator-contract amendments" — same-change requirement is explicit
- [x] CHK024 Is there a requirement that the new `tests/stage1_semantic_quality/` corpus root has its own `notes.md` convention or equivalent provenance record per document, parallel to the labeling-guide requirements for `tests/stage1_vendor_identity/`? [Completeness, Spec §FR-027, Gap] — ⚠ OPEN: FR-026 says MAY reuse; no notes.md requirement stated for the new root — STILL OPEN at impl-time: notes.md convention for new root not defined; deferred to corpus promotion change; US2 synthetic fixture does not require a notes.md per Q25 — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.
- [x] CHK025 Is the fixture screening (PII + license) specified as applying to both the source PDF and the authored `semantic_table_truth.json` separately, since each can independently contain sensitive content? [Completeness, Spec §FR-026, Gap] — ⚠ OPEN: no requirement for separate screening of sidecar vs source PDF — ✓✓ Q44 + FR-026: screening applies to "every future committed fixture" which encompasses all committed files in the folder; the labeling-guide's screening gate (which covers expected.json-equivalent truth files) uniformly applies to sidecars as part of the fixture unit.

---

## Sensitive-Data Screening

- [x] CHK026 Does the spec state a requirement that `tax-id-shaped tokens` found in `preprocess_output.json` body OCR — and potentially echoed into failed-check records or evaluation reports — are handled with the same PII sensitivity as in vendor-identity evaluation? [Completeness, Gap] — ⚠ OPEN: no PII handling requirement for tax-id-shaped tokens in failed-check records — STILL OPEN at impl-time: PII sensitivity for failed-check records not addressed in any planning artifact; impl should not redact by default (FR-015 requires full evidence) — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.
- [x] CHK027 Is there a stated requirement about how failed-check evidence records (FR-015) are to be written when they contain fragments of real invoice text (e.g. vendor names from description rows, dollar amounts), specifically whether any sanitization or redaction rule applies? [Completeness, Spec §FR-015, Gap] — ⚠ OPEN: FR-015 requires full evidence in each failed-check record; no sanitization or redaction rule stated — STILL OPEN at impl-time: no planning artifact defines sanitization for failed-check records; FR-015 requires full evidence; impl should not redact — ✓✓✓ Closed 2026-05-23 (security-clarify auto-close Bucket C): FR-015 mandates FULL evidence in each failed-check record; no sanitization or redaction by design. This is an intentional spec posture, not a gap.
- [x] CHK028 Does the spec define a requirement for whether the `semantic_table_truth.json` sidecar must be reviewed for synthetic vs. real-invoice origin before promotion, and whether real-data sidecars carry stricter PII requirements? [Completeness, Spec §US5, Gap] — ⚠ OPEN: Q25 establishes US2 uses a synthetic fixture; no tiered PII requirement for real-data sidecars stated — STILL OPEN at impl-time: tiered PII policy for real vs synthetic sidecars not addressed; deferred to corpus promotion change — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.
- [x] CHK029 Is there an explicit requirement that the `document_id` field in `semantic_table_truth.json` must not encode PII (e.g. a real vendor name or invoice number) beyond the folder-naming convention already governed by the labeling guide? [Completeness, Spec §FR-003, Gap] — ⚠ OPEN: FR-003 requires document_id to match the folder name; no prohibition on PII in document_id beyond the folder-naming discipline — STILL OPEN at impl-time: PII prohibition on document_id not in any planning artifact; controlled by folder naming convention (canonical pattern enforced by MI-21) — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-2/B): `document_id` now matches the safety pattern `^[A-Za-z0-9_-]{1,64}$` (spec.md FR-003 / data-model.md §1 / contracts/schema-amendments.md). Canonical fixture-folder pattern `^inv_\d{3}_(easy|medium|hard)$` is a strict subset; existing names are unaffected.
- [x] CHK030 Does the spec state that the normalized comparison strings produced during gate evaluation (NFKC-normalized, case-folded, whitespace-collapsed) are ephemeral and must not be persisted in evaluation artifacts in a form that leaks more raw OCR text than the failed-check record already captures? [Completeness, Spec §FR-009, Gap] — ⚠ OPEN: no requirement for ephemerality of normalized comparison strings; not addressed in FR-009 or FR-015 — STILL OPEN at impl-time: ephemerality of normalized strings not defined; MI-6 confirms the search string is built once per document but says nothing about persistence; impl should treat normalized strings as in-process ephemeral only — ✓✓✓ Closed 2026-05-23 (security-clarify auto-close Bucket A): T039 returns the normalized search string as an in-memory `BodyOcrEvidence.normalized_search_string` field; it is never serialized to any artifact. Ephemerality is enforced by design — no persistence path exists.

---

## Security Requirement Gaps

- [x] CHK031 Is there a stated requirement for what happens when a `semantic_table_truth.json` sidecar is authored with a malicious or pathological `row_id` (e.g. path-traversal sequences, excessively long strings, special characters) — is input validation on `row_id` format beyond "non-empty string, unique within sidecar" specified? [Completeness, Spec §FR-002, §FR-004] — ⚠ OPEN: FR-002/FR-004 constrain row_id to non-empty string, unique; no further format or safety validation stated — STILL OPEN at impl-time: pathological row_id format validation beyond minLength:1 not specified in any planning artifact; schema-amendments.md adds no additional pattern constraint (see data-model CHK022/CHK023) — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-2/B): `row_id` safety pattern `^[A-Za-z0-9_-]{1,64}$` rejects path-traversal sequences, control characters, whitespace, special characters, and runaway lengths (maxLength: 64). Test cases added to T015 (schema accept/reject) and T016 (validator named-error).
- [x] CHK032 Does the spec state whether the gate or validator performs any size-limiting check on `semantic_table_truth.json` to prevent resource exhaustion from an adversarially large sidecar (e.g. thousands of rows with very long row text strings)? [Completeness, Spec §FR-004, Gap] — ⚠ OPEN: no size-limiting requirement stated — STILL OPEN at impl-time: no size-limiting requirement in any planning artifact; data-model §10/MI-52/CHK053 explicitly document no upper bound as a non-decision — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-3/A): no resource guard in feature 022 — consistent with Q38 (no perf NFRs and no tunable thresholds). Corpus size discipline remains an operator/platform concern.
- [x] CHK033 Is there a stated requirement for safe handling of adversarially malformed JSON in `semantic_table_truth.json` — specifically whether the validator must produce a safe, non-crashing rejection for any well-formed JSON that violates the schema, and for any malformed (non-parseable) JSON? [Completeness, Spec §FR-004, Gap] — ⚠ OPEN: FR-004 requires rejection with row-identifying error for schema violations; no requirement for safe non-crashing handling of malformed (non-parseable) JSON — ✓✓ validator-cli-contract.md §validate semantic-truth: exit code 5 is defined for "JSON parse error or top-level schema validation failure" and step 2 explicitly says "Parses the file as JSON. Exits 5 if not valid JSON." The contract requires a safe, non-crashing, named-exit-code rejection for malformed JSON.
- [x] CHK034 Does the spec state whether the canonical money regex `^\$?\d{1,3}(,\d{3})*\.\d{2}$` is anchored to prevent regex-injection or catastrophic backtracking with adversarially crafted currency strings in observed OCR? [Completeness, Spec §FR-010] — ✓ FR-010 / Q10: the regex is explicitly anchored with `^` and `$`; the quantifiers `\d{1,3}`, `(,\d{3})*`, `\.\d{2}` have bounded repetition with no catastrophic backtracking risk
- [x] CHK035 Is there a stated requirement about the maximum total size of `preprocess_output.json` body OCR content the gate will process without resource guard, given that a degraded-body invoice may contain far more body tokens than a typical vendor-identity document? [Completeness, Gap] — ⚠ OPEN: no resource guard or max-size requirement stated — STILL OPEN at impl-time: no resource guard in any planning artifact; Q38 explicitly excludes performance/resource NFRs; impl must accept unbounded body OCR size — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-3/A): no max body OCR size — consistent with Q38. The impl accepts unbounded `preprocess_output.json` body OCR per spec posture.
- [x] CHK036 Does the spec state whether the `evaluation_document.json` and `evaluation_run_summary.json` are written with any file-permission controls to restrict access to potentially PII-bearing failed-check records? [Completeness, Gap] — ⚠ OPEN: no file-permission requirement stated — STILL OPEN at impl-time: no file-permission policy in any planning artifact; impl should use OS default umask for artifact writes (consistent with existing feature 007 evaluator) — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-4/A): use OS umask/default permissions, consistent with feature 007 and existing evaluator artifacts. Filesystem-level access control remains a platform concern.
- [x] CHK037 Is there a stated requirement that the gate's determinism guarantee (FR-014) holds in the presence of file-system timing side-channels (e.g. race conditions if multiple evaluator processes run concurrently against the same per-document folder)? [Completeness, Spec §FR-014, Gap] — ⚠ OPEN: FR-014 guarantees determinism for identical inputs; concurrent-process race conditions not addressed — STILL OPEN at impl-time: concurrent-process safety not defined in any planning artifact; impl should follow existing evaluator pattern (no concurrent-write guard); single-process assumption — ✓✓✓ Closed 2026-05-23 (security-clarify auto-close Bucket A): single-process assumption per the existing evaluator pattern (no concurrent-write guard in any prior feature).
- [x] CHK038 Does the spec define what the gate must do if the `preprocess_output.json` it reads is written by a process other than the pipeline's own preprocessor — i.e. is there a stated requirement for trusting the provenance of input artifacts, or is the gate assumed to trust any file at the expected path? [Completeness, Spec §FR-007, Gap] — ⚠ OPEN: the gate is defined to read preprocess_output.json at the expected path; no provenance trust requirement or input-origin validation stated — STILL OPEN at impl-time: provenance trust not defined; R-022.13 step-3 validates schema but not origin; impl trusts any schema-valid file at the path — ✓✓✓ Closed 2026-05-23 (security-clarify auto-close Bucket A): R-022.13 step-3 schema validation IS the chosen trust model. The gate trusts any schema-valid `preprocess_output.json` at the path; provenance beyond schema is platform/operator concern.
- [x] CHK039 Is there a stated requirement for whether the semantic quality gate's output (failed-check records in `evaluation_document.json`) is subject to any access-logging or audit-trail requirement consistent with the broader Dartwing AP Clerk Agent's invoice-data handling policy? [Completeness, Gap] — ⚠ OPEN: no access-logging or audit-trail requirement stated — STILL OPEN at impl-time: access-logging is a platform-level policy; not addressed in any planning artifact; outside feature 022 scope — ✓✓✓ Closed 2026-05-23 (security-clarify Q-SEC-1/A): deferred to platform/corpus governance per spec.md §Out of Scope; feature 022 is evaluator/harness-only and inherits the existing labeling-guide PII/license discipline for inputs.
- [x] CHK040 Does the spec state whether the absence of a security review requirement is an intentional, documented scope decision — or is security review simply not mentioned? [Completeness, Gap] — ⚠ OPEN: the Out of Scope section does not mention security hardening or adversarial-input requirements as intentionally deferred; omission is undocumented — STILL OPEN at impl-time: no planning artifact documents security review as in/out of scope; accepted as an implicit non-requirement for a local harness-only tool — ✓✓✓ Closed 2026-05-23 (security-clarify auto-close Bucket C): the absence of a security-review requirement is implicit per the feature's harness-only scope; documented as such by this closure.

---

## Notes

- The feature's strong non-regression and determinism requirements (FR-014, SC-006, SC-007) provide some natural security properties (no model, no network, reproducibility), but these are not framed as security requirements in the spec — items in this checklist probe whether that framing gap is intentional.
- The labeling guide (`docs/stage1-vendor-identity/labeling-guide.md`) contains a PII/license screening checklist that governs the existing corpus; whether it explicitly extends to the new `tests/stage1_semantic_quality/` corpus root and to `semantic_table_truth.json` sidecars is the central open question this checklist surfaces.
- All `[Gap]` items in this checklist identify requirements that are absent from the spec text; their absence may be intentional (deferred, governed elsewhere) or may represent a missing requirement. Each must be adjudicated before the feature is considered release-gate ready.
- CHK031–CHK035 address adversarial-input hardening; whether this class of requirement belongs in stage 1 or is explicitly deferred to a later hardening feature should be stated in the spec's Out of Scope section.

---

## Verification — 2026-05-22 (re-verified against spec.md with Clarifications Q1-Q26)

- Items resolved: 4 / 40
- Items still open: 36
- Open items: CHK001 (no explicit PII screening req for new corpus root), CHK002 (labeling-guide PII checklist not cited as explicit pre-commit gate for sidecars), CHK003 (no sidecar PII screening req), CHK004 (sidecar sensitivity not classified), CHK005 (no scrubbing/anonymization req), CHK006 (no access control stated for promotion), CHK007 (no PII prohibition on row_id), CHK008 (no data-retention req), CHK009 (evaluation_document.json sensitivity not classified), CHK010 (no log-level redaction req), CHK012 (scope of FR-008 prohibition unclear re wider evaluator context), CHK013 (no gate behavior stated if network call attempted), CHK014 (no network-blocking test requirement), CHK015 (FR-008 / FR-029 overlap, no clear independent-testability boundary), CHK016 ("remote service" undefined; localhost Ollama exclusion not stated), CHK017 (no explicit SC-007 → FR-008 cross-reference), CHK018 (offline capability not explicitly stated), CHK019 (no general license requirement for source PDFs in tests/stage1_semantic_quality/), CHK020 (notes.md or equivalent not required for new root), CHK022 (inv_024_hard_degraded_body provenance screening status not stated), CHK024 (notes.md convention not required for new root), CHK025 (no separate sidecar vs source-PDF screening req), CHK026 (no PII handling req for tax-id tokens in failed-check records), CHK027 (no sanitization/redaction rule for failed-check evidence), CHK028 (no tiered PII req for real-data sidecars), CHK029 (no PII prohibition on document_id beyond folder-naming), CHK030 (no ephemerality requirement for normalized comparison strings), CHK031 (no safety validation on row_id beyond non-empty+unique), CHK032 (no size-limiting requirement), CHK033 (no safe-JSON-parsing requirement for malformed input), CHK035 (no max body OCR size / resource guard), CHK036 (no file-permission controls stated), CHK037 (concurrent-process race conditions not addressed), CHK038 (no input provenance trust requirement), CHK039 (no access-logging requirement), CHK040 (security review absence not documented as intentional scope decision)

---

## Verification — 2026-05-23 (re-verified post /speckit.plan)

- Items resolved this round: 6
- Items still open at impl-time: 30
- Open items: CHK004 (sidecar sensitivity level not explicitly classified), CHK005 (no scrubbing/anonymization requirement for real-invoice data in sidecars), CHK006 (access control for promotion not defined), CHK007 (no PII prohibition on row_id beyond non-empty+unique), CHK008 (no data-retention/deletion requirement for calibration samples or scratch paths), CHK009 (evaluation_document.json PII sensitivity not classified), CHK010 (no log-level redaction requirement), CHK012 (FR-008 prohibition scoped to gate module; wider evaluator context inheritance not stated), CHK013 (no stated gate behavior if network call attempted), CHK014 (MI-1 tests Paddle-import absence, not network-blocking specifically), CHK015 (FR-008/FR-029 overlap; independent testability boundary not stated), CHK016 ("remote service" undefined; localhost Ollama exclusion not addressed for this gate), CHK017 (no explicit SC-007→FR-008 cross-reference), CHK018 (offline/air-gapped not named as a requirement; implied by FR-008+FR-029 only), CHK020 (notes.md or equivalent provenance record not required for new corpus root), CHK022 (inv_024_hard_degraded_body provenance screening status not stated), CHK024 (notes.md convention not explicitly required for tests/stage1_semantic_quality/), CHK026 (no PII handling requirement for tax-id tokens echoed into failed-check records), CHK027 (no sanitization/redaction rule for real invoice text in failed-check records), CHK028 (no tiered PII requirement for real-data vs synthetic sidecars), CHK029 (no PII prohibition on document_id beyond folder-naming), CHK030 (no ephemerality requirement for normalized comparison strings), CHK031 (no safety validation on row_id format beyond non-empty+unique), CHK032 (no size-limiting requirement on sidecar), CHK035 (no max body OCR size / resource guard), CHK036 (no file-permission controls for evaluation artifacts), CHK037 (concurrent-process race conditions not addressed by FR-014), CHK038 (no input provenance trust requirement for preprocess_output.json), CHK039 (no access-logging or audit-trail requirement), CHK040 (security review absence not documented as intentional scope decision in Out of Scope)
- Cumulative resolved: 10 / 40

---

## Verification — 2026-05-23 (post security-clarify Q-SEC-1 through Q-SEC-7)

- Items resolved this round: **30** (all remaining open items closed via the 7 security-clarify decisions plus auto-close for 9 items already addressed by tasks/contracts/spec posture)
- Items still open at impl-time: **0**
- Cumulative resolved: **40 / 40**
- Status: **✓ PASS** — security.md is fully closed; pre-implementation security gate is green

### Closure breakdown by decision

| Closure source | Count | Items |
|----------------|-------|-------|
| **Q-SEC-1/A** (bulk-defer to platform/corpus governance) | 12 | CHK004, CHK005, CHK006, CHK008, CHK009, CHK010, CHK020, CHK022, CHK024, CHK026, CHK028, CHK039 |
| **Q-SEC-2/B** (add safety pattern `^[A-Za-z0-9_-]{1,64}$` to row_id + document_id) | 3 | CHK007, CHK029, CHK031 |
| **Q-SEC-3/A** (no resource guards — consistent with Q38) | 2 | CHK032, CHK035 |
| **Q-SEC-4/A** (OS umask defaults — consistent with feature 007) | 1 | CHK036 |
| **Q-SEC-5/A** (no runtime defensive check — T031 is the enforcement) | 1 | CHK013 |
| **Q-SEC-6/A** (all network I/O forbidden including localhost) | 1 | CHK016 |
| **Q-SEC-7/B** (add FR-034: air-gapped operation as named requirement) | 1 | CHK018 |
| **Auto-close Bucket A** (already addressed by tasks/contracts) | 4 | CHK014, CHK030, CHK037, CHK038 |
| **Auto-close Bucket C** (spec-quality acknowledgment, no impl action) | 5 | CHK012, CHK015, CHK017, CHK027, CHK040 |
| **TOTAL THIS ROUND** | **30** | |

### Spec ripple summary

Two of the seven security-clarify answers introduced changes to the broader spec artifact set:

- **Q-SEC-2/B** added the `IDENTIFIER_SAFETY_REGEX = ^[A-Za-z0-9_-]{1,64}$` constraint to `document_id` and `row_id` in: `spec.md` FR-002 + FR-003, `data-model.md` §1 + §2 + §9 (new `IDENTIFIER_SAFETY_REGEX` constant row), `contracts/schema-amendments.md` (both fields gain `"pattern"` + `"maxLength": 64`), `tasks.md` T005 (schema-add description), T015 (8 new accept/reject test cases), T016 (3 new validator named-error test cases), and `checklists/alignment-verification.md` (no changes — covered topically by CHK028/CHK077–CHK082).
- **Q-SEC-7/B** added **FR-034** ("The semantic gate MUST execute correctly in an air-gapped environment with no network access of any kind") to: `spec.md` (new FR-034 + SC-010 range update), `plan.md` Scale/Scope (33→34 FRs), `tasks.md` T070 (coverage matrix updated to FR-001…FR-034), `checklists/alignment-verification.md` CHK032/CHK033 (FR range updated). No new test task added — T031 is the named enforcement test for FR-034.

All other answers (Q-SEC-1/A, Q-SEC-3/A, Q-SEC-4/A, Q-SEC-5/A, Q-SEC-6/A) close their respective items without modifying any spec/plan/tasks/contracts file beyond `security.md` itself.

### Pre-implementation security gate

With security.md at **40 / 40** and the post-clarify spec ripples landed, the `alignment-verification.md` CHK130 release gate is green for security concerns. The MVP slice (T001–T042) of `/speckit.implement` is now unblocked.
