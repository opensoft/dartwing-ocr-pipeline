# Scope Requirements Quality Checklist: OCR Semantic Quality Gate

**Purpose**: Validate that scope-boundary, non-regression, runtime-deferral, and migration requirements are complete, unambiguous, and internally consistent.
**Created**: 2026-05-22
**Feature**: [spec.md](../spec.md)

---

## Scope Boundary Clarity

- [ ] CHK001 Does FR-028 define "rewrite in place" precisely enough to be objectively applied — e.g. does it prohibit any modification to the source files of features 019, 020, and 021, or only modifications that change their observable behavior? [Clarity, Spec §FR-028] — ⚠ OPEN: "rewrite in place" is not operationally defined; the parenthetical "adds a new, separately-surfaced capability" is explanatory but not a testable criterion — STILL OPEN at impl-time: Plan §Constitution-Check clarifies the gate lives in `evaluator/` and `validator/` only, but "rewrite in place" as a phrase remains undefined; implementers rely on the file-path boundary (CHK002/CHK003 now resolved)
- [x] CHK002 Is the boundary between "evaluator/harness-owned" and "pipeline-owned" code spelled out in terms of module or package paths so an implementer can determine unambiguously which files belong to which side? [Completeness, Spec §Scope Discipline note, §Assumptions] — ⚠ OPEN: the spec uses "evaluator/harness-owned" but names no concrete module or package paths — ✓✓ Plan §Constitution-Check Principle I + Plan §Source-Code: harness-owned = `src/dartwing_ocr/evaluator/` + `src/dartwing_ocr/validator/`; pipeline-owned = `src/dartwing_ocr/preprocessing/`, `src/dartwing_ocr/extract/`, `src/dartwing_ocr/router/`, `src/dartwing_ocr/assembler/` (all explicitly listed as NOT touched)
- [x] CHK003 Does the spec define which specific source modules the semantic gate is permitted to add or modify versus which it MUST NOT touch (e.g. may not touch any file under `src/dartwing_ocr/preprocessing/`, `src/dartwing_ocr/extract/`, or `src/dartwing_ocr/router/`)? [Completeness, Spec §FR-028, §FR-033] — ⚠ OPEN: no concrete file-system or package-path prohibition is stated — ✓✓ Plan §Source-Code tree enumerates every permitted file by name (12 evaluator/ files, 4 validator/ files, 3 contract_versions files); Plan §Constitution-Check Principle I explicitly states "does NOT touch src/dartwing_ocr/preprocessing/, extract/, router/, or assembler/"
- [x] CHK004 Does FR-030 define "line-item extraction" precisely enough to distinguish it from "table/body quality evaluation" — i.e. is there a boundary criterion such that a reviewer can decide which side a proposed addition falls on? [Clarity, Spec §FR-030] — ✓ FR-030
- [ ] CHK005 Does the Out of Scope section list "Jetson / edge deployment" — and is there a requirement that no new Jetson-specific configuration or conditional code path be introduced by this feature? [Completeness, Spec §Out of Scope] — ⚠ OPEN: Out of Scope mentions Jetson but no normative FR prohibits introducing Jetson-specific code paths — STILL OPEN at impl-time: Plan §Technical Context confirms no GPU/Jetson dependency; no normative FR prohibits Jetson code; covered implicitly by FR-029 (no new model runtime) and the CPU-only constraint, but not explicitly
- [x] CHK006 Is there a requirement that no new CLI flag is introduced (per Clarifications Q13), and is that prohibition stated in the functional requirements rather than only in the clarifications narrative? [Completeness, Spec §FR-007, §Clarifications Q13] — ✓ FR-007 / Q13
- [x] CHK007 Does the spec confirm that the semantic gate's `tests/stage1_semantic_quality/` corpus root is owned exclusively by this feature's evaluator path and is not consumed by the vendor-identity baseline evaluation? [Clarity, Spec §FR-026, §Clarifications Q1, Q15] — ✓ FR-026 / Q1 / Q15
- [x] CHK008 Does the spec define whether the semantic corpus root at `tests/stage1_semantic_quality/` reuses the same per-document folder contract as `tests/stage1_vendor_identity/` or a new variant, and if a new variant, is it specified? [Completeness, Spec §FR-026] — ⚠ OPEN: FR-026 says the root "MAY reuse the existing per-document folder discipline where useful" but does not specify whether it does or what the contract is — ✓✓ Q40 + FR-026: "same canonical inv_NNN_<difficulty> pattern" used; Q23 allowlist applies to both roots; Schema-Amendments §folder.schema.json shows `semantic_table_truth.json` added as optional; the per-document folder contract is the v1.3.0 folder.schema.json which governs both roots
- [x] CHK009 Is there a requirement bounding which artifact files the semantic gate is permitted to read — e.g. is it restricted to `preprocess_output.json` and `semantic_table_truth.json` only, with other pipeline artifacts forbidden as inputs? [Completeness, Spec §FR-007, §Assumptions] — ✓ FR-007 / Assumptions
- [x] CHK010 Does FR-029 enumerate every prohibited addition (learned classifier, new model runtime, remote service, network call, prompt-owned routing decision) in sufficient detail to rule out edge cases such as calling a local REST API not backed by a model? [Completeness, Spec §FR-029] — ⚠ OPEN: FR-029 lists five categories but a local REST API not backed by a model is not clearly excluded — ✓✓ FR-008 + MI-1: "MUST NOT perform network I/O, open sockets, resolve hostnames, or make HTTP requests at any time during a gate evaluation" — this is unconditional and covers any REST API call regardless of whether a model backs it

---

## Out-of-Scope Completeness

- [x] CHK011 Does the Out of Scope section explicitly exclude adding new top-level pipeline CLI subcommands (e.g. a standalone `dartwing-semantic-gate` command), or is the prohibition only implied by the evaluator-integration approach? [Completeness, Spec §Out of Scope, §FR-007] — ⚠ OPEN: FR-007 prohibits a new CLI flag but not a new subcommand; Out of Scope does not explicitly exclude standalone commands — ✓✓ MI-18 + Plan §Technical Context: "No new top-level package, no new CLI entry point introduced (per Clarifications Q13)"; Validator-CLI-Contract §Notes: "No new top-level CLI entry point is introduced"; R-022.8: "No new `dartwing-validate-sidecar` entry point (rejected: Q13)"
- [x] CHK012 Does the Out of Scope section explicitly exclude changes to `routing_decision.json`'s schema or its file-write logic? [Completeness, Spec §Out of Scope, §FR-033] — ✓ FR-033
- [x] CHK013 Does the Out of Scope section explicitly exclude changes to `final_structured_payload.json`'s schema or its assembler logic? [Completeness, Spec §Out of Scope, §FR-033] — ⚠ OPEN: FR-033 names `routing_decision.json` and "runtime review policy" explicitly but does not name `final_structured_payload.json` schema or assembler — ✓✓ Plan §Constitution-Check Principle I: "does NOT touch src/dartwing_ocr/assembler/"; Plan §Source-Code lists "(everything else unchanged)"; FR-031 confines schema changes to the two evaluation report artifacts + the new sidecar — `final_structured_payload.schema.json` is COPIED unchanged from v1.2.0 per Schema-Amendments §Schemas unchanged
- [x] CHK014 Does the Out of Scope section explicitly exclude changes to `edge_extraction_output.json`'s schema or voter logic? [Completeness, Spec §Out of Scope, §FR-028] — ✓ FR-031
- [x] CHK015 Does the Out of Scope section explicitly exclude regenerating committed baseline evaluation artifacts (e.g. `evaluation_document.json` and `evaluation_run_summary.json`) for the existing 20-document corpus? [Completeness, Spec §Out of Scope, §Assumptions] — ✓ Out of Scope / Assumptions
- [x] CHK016 Is "a learned classifier" defined narrowly enough that a deterministic regex or rule-based scoring module is clearly on the in-scope side, and a statistical model is clearly out of scope? [Clarity, Spec §Out of Scope, §FR-029] — ✓ FR-029 / Out of Scope
- [x] CHK017 Does the Out of Scope section address whether this feature may amend `contracts/stage1_vendor_identity/AMENDMENTS.md` beyond the minor bump for the sidecar schema — or is it restricted to exactly the one stated amendment? [Completeness, Spec §FR-031, §Out of Scope] — ✓ FR-031
- [x] CHK018 Does the Out of Scope section address changes to `docs/stage1-vendor-identity/labeling-guide.md` — is updating it in scope as part of the corpus-governance work (FR-027) or explicitly excluded? [Ambiguity, Spec §Out of Scope, §FR-027] — ⚠ OPEN: FR-027 says "validator and documentation MUST treat…" implying doc updates are in scope, but Out of Scope does not address labeling-guide specifically — ✓✓ Quickstart Notes: "For sidecar authoring patterns, see docs/stage1-vendor-identity/labeling-guide.md"; Plan §Source-Code lists `docs/stage1-vendor-identity/prd-ocr-semantic-quality-gate.md` as CHANGED and `schemas.md` as CHANGED — labeling-guide updates are in scope per FR-027 "documentation MUST treat" requirement; Q44 affirms same PII screening applies

---

## Non-Regression Requirements

- [x] CHK019 Does FR-021 specify what "feature 019 OCR-only fallback behavior" means precisely enough to write a non-regression test — e.g. does it cite the specific FR-005 token-count / mean-confidence trigger values that MUST remain unchanged? [Measurability, Spec §FR-021] — ✓ FR-021
- [x] CHK020 Does FR-022 enumerate the specific feature-020 run-summary fields that MUST NOT change — or does it say only "its existing run-summary fields" without naming them? [Completeness, Spec §FR-022] — ⚠ OPEN: FR-022 says "its existing run-summary fields" without naming them — ✓✓ MI-22 names the specific fields: `vendor_identity_passed`, `evidence_gate_id`, `evidence_gate_state_counts`, `evidence_gate_documents`, `evidence_gate_suppressed_fallback_count`, and all feature-019/021 fields; SC-006 with Quickstart Path 7 diff command provides the measurable enforcement
- [ ] CHK021 Does FR-023 specify what "feature 021 GPU MVP promotion criteria" means precisely enough to write a non-regression test — e.g. does it reference the specific performance thresholds or benchmark criteria that MUST remain unchanged? [Measurability, Spec §FR-023] — ⚠ OPEN: FR-023 says "feature 021 GPU MVP promotion criteria" without citing specific thresholds — STILL OPEN at impl-time: Plan states "Feature 021 GPU MVP promotion criteria unaffected" but does not cite specific thresholds; the gate is CPU-only so cannot regress GPU benchmark thresholds; testability still insufficient for a precise non-regression test
- [x] CHK022 Does SC-006 define "identical" — e.g. does it require byte-identical JSON output or only field-value equality, and does it cover field order? [Measurability, Spec §SC-006] — ⚠ OPEN: SC-006 says "identical" without specifying byte-identical vs. field-value equality — ✓✓ MI-22 "MUST be byte-identical before and after"; Quickstart Path 7 uses `sha256sum` + `diff` for verification; MI-16 + Q34 stable JSON serialization (sorted keys, fixed-precision floats) pins field order and byte representation
- [x] CHK023 Is the non-regression scope in SC-006 defined to cover feature-020 observability surface fields specifically (e.g. `evidence_gate_id`, `evidence_gate_state_counts`, `evidence_gate_documents`, `evidence_gate_suppressed_fallback_count`) or only the general "vendor-identity pass/fail"? [Completeness, Spec §SC-006, §FR-022] — ✓ SC-006
- [x] CHK024 Does FR-024 specify what "the meaning or values of `document_pass_fail.vendor_identity_passed`" means — e.g. does it require that the boolean logic computing `vendor_identity_passed` is unmodified, not just that the output values happen to match? [Clarity, Spec §FR-024] — ⚠ OPEN: FR-024 says "meaning or values" but does not specify whether this requires logic-level preservation or output-value equivalence — ✓✓ MI-22 "MUST be byte-identical before and after this feature lands"; Plan §Constitution-Check Principle I confirms no evaluator scoring/comparison modules that compute vendor_identity_passed are touched; both logic-level preservation (no source change) and output equivalence (byte-identical SC-006) are enforced together
- [ ] CHK025 Is there a requirement that the feature does not add any import-time side effect or module-level initialization to existing preprocessing, extraction, or routing modules? [Completeness, Gap] — ⚠ OPEN: no such requirement exists in the spec — STILL OPEN at impl-time: MI-1 prohibits Paddle import in the gate modules; Plan lists no changes to preprocessing/extract/router modules; but no explicit normative statement prohibits import-time side-effects being added to those existing modules
- [x] CHK026 Is the non-regression corpus used in SC-006 specified precisely — e.g. must it be the canonical 20 documents under `tests/stage1_vendor_identity/` run with a specific profile, or is "the corpus" left undefined? [Measurability, Spec §SC-006, §US4] — ⚠ OPEN: SC-006 names "20-document vendor-identity baseline corpus" but does not specify the profile or evaluator invocation — ✓✓ Quickstart Path 7: exact invocation `python -m dartwing_ocr.evaluator evaluate-corpus tests/stage1_vendor_identity/` with `diff` verification; corpus = `tests/stage1_vendor_identity/` (20 docs, no sidecars); gate is CPU-only with no model/voter-config dependency so profile ambiguity is moot
- [x] CHK027 Does US4 AS1 define what runtime conditions must be held constant for the non-regression comparison to be valid (e.g. same voter config, same model, same profile)? [Completeness, Spec §US4] — ⚠ OPEN: US4 AS1 says "same corpus" but does not fix voter config, model, or profile — ✓✓ The semantic gate is a pure evaluator extension with no model/voter-config dependency; evaluating `tests/stage1_vendor_identity/` (no sidecars) requires no Ollama/model; the runtime-condition ambiguity is resolved by the gate's CPU-only nature — there is no voter config relevant to this evaluator path
- [ ] CHK028 Is there a requirement that the feature's code does not modify the `RunSummary.SCHEMA_VERSION` beyond the additive fields already required (FR-018), to prevent accidental version drift that would break existing consumers? [Completeness, Gap] — ⚠ OPEN: no such requirement exists in the spec — STILL OPEN at impl-time: Plan bumps `contract_versions.py::CURRENT_CONTRACT_SET_VERSION` to 1.3.0; no explicit guard against accidental further version drift beyond that controlled bump

---

## Runtime-Integration Deferral

- [x] CHK029 Does FR-033 define "runtime behavior" precisely — e.g. does it cover only the pipeline's runtime decision paths, or does it also cover any side effect that influences what the pipeline writes to `routing_decision.json`? [Clarity, Spec §FR-033] — ✓ FR-033
- [x] CHK030 Does FR-033 prohibit the semantic gate from writing any artifact other than the report-surface fields on `evaluation_document.json` / `evaluation_run_summary.json` — e.g. could the gate write a `semantic_verdict.json` artifact into the per-document folder? [Completeness, Spec §FR-033, §Out of Scope] — ⚠ OPEN: FR-033 scopes output to evaluator reports but does not explicitly prohibit writing a per-document `semantic_verdict.json` artifact — ✓✓ Plan §Technical Context storage: "writes evaluation_document.json (extended with semantic fields) and corpus-root/evaluation_run_summary.json"; Data-Model §13 side-effects list only these two outputs; no `semantic_verdict.json` in Plan §Source-Code; FR-031 caps artifact changes to "the new optional sidecar contract and optional additive semantic-quality fields on evaluation reports"
- [x] CHK031 Does the spec prohibit writing ANY file into a per-document folder in `tests/stage1_vendor_identity/` as a side effect of the semantic gate? [Completeness, Gap] — ⚠ OPEN: no explicit prohibition on writing to vendor-identity per-document folders — ✓✓ Plan §Technical Context storage: gate "writes evaluation_document.json … and corpus-root/evaluation_run_summary.json"; Quickstart Path 7 shows vendor-identity corpus evaluation read-only with output at corpus root; SC-006 byte-identical assertion would fail if any per-doc file changed; Data-Model §13 side-effects confirms two output files only
- [x] CHK032 Does Clarifications Q3 (runtime integration deferred) appear in a normative functional requirement (FR-033), or is it only stated in the clarifications narrative — and is the normative placement sufficient to make it testable? [Traceability, Spec §Clarifications Q3, §FR-033] — ✓ FR-033 / Q3
- [ ] CHK033 Does FR-033 clarify whether "MUST NOT alter … runtime review policy" means the feature cannot update documentation, configuration files, or code that describes when human review is triggered, or only that it cannot change automated runtime decisions? [Clarity, Spec §FR-033] — ⚠ OPEN: "runtime review policy" is not distinguished from documentation or config describing review triggers — STILL OPEN at impl-time: Plan includes docs updates (prd-ocr-semantic-quality-gate.md, schemas.md) but no new configuration; "runtime review policy" interpretation left to implementer
- [x] CHK034 Is there a requirement that a "whole-invoice/table-quality failed" report message (which FR-033 permits) MUST NOT be surfaced in any field that existing consumers of `routing_decision.json` or `final_structured_payload.json` would read? [Completeness, Spec §FR-033] — ⚠ OPEN: FR-033 says verdict is evaluator/harness/report-only but does not explicitly name prohibited field paths in pipeline artifacts — ✓✓ Plan §Source-Code: gate modules are all under `evaluator/`; `routing_decision.schema.json` and `final_structured_payload.schema.json` are COPIED unchanged (Schema-Amendments §Schemas unchanged); `src/dartwing_ocr/router/` and `assembler/` are listed as NOT touched — these schemas cannot carry new semantic fields without changes to those modules
- [ ] CHK035 Does the spec define how the deferral of runtime integration will be signaled to future feature owners — e.g. is there a required TODO comment, architecture decision record, or OpenSpec item to track the deferred work? [Completeness, Gap] — ⚠ OPEN: no tracking mechanism required — STILL OPEN at impl-time: FR-033 states deferral in prose; no required ADR, TODO comment, or OpenSpec item is mandated

---

## Report-Only Boundary

- [x] CHK036 Does FR-033 specify what counts as "report-only" output — e.g. does it mean only `evaluation_document.json` and `evaluation_run_summary.json`, or does it also include stdout log lines and run_summary stdout lines? [Clarity, Spec §FR-033] — ⚠ OPEN: FR-033 says "evaluator/harness/report-only at landing" but does not define whether stdout run_summary lines are in or out of scope — ✓✓ Evaluator-Output-Contract defines the semantic output surface as exactly `evaluation_document.json` extensions and `evaluation_run_summary.json` extensions; Plan §Technical Context: "writes evaluation_document.json … and corpus-root/evaluation_run_summary.json"; the feature-015 run_summary stdout line is unchanged (the gate's aggregate metrics go to the JSON file, not the existing stdout protocol)
- [ ] CHK037 Is there a requirement that the semantic verdict is NOT propagated to stdout run_summary lines in any way that would alter feature-015/016/017/018/019 run_summary consumers? [Completeness, Spec §FR-033, Gap] — ⚠ OPEN: no such requirement exists — STILL OPEN at impl-time: no normative FR or MI prohibits adding semantic fields to the existing run_summary stdout line; implementers must decide
- [ ] CHK038 Does the spec prohibit the semantic gate from setting any process exit code that differs from what the evaluator would emit without the gate present (i.e. a failing semantic verdict MUST NOT cause a non-zero evaluator exit code)? [Completeness, Gap] — ⚠ OPEN: no exit-code requirement exists — STILL OPEN at impl-time: no FR or MI addresses evaluator exit code behavior when semantic gate fails; implementers must decide
- [x] CHK039 Is the "report-only" boundary stated such that a reviewer can decide unambiguously whether a proposed design change (e.g. writing semantic status to a new sidecar artifact) violates it? [Measurability, Spec §FR-033] — ⚠ OPEN: "report-only" is not operationally defined with enough specificity to decide edge cases — ✓✓ Evaluator-Output-Contract + Data-Model §13 state-transitions/side-effects: permitted outputs are exactly `evaluation_document.json` (semantic fields added) and `evaluation_run_summary.json` (semantic_table_quality_metrics namespace added); FR-031 caps artifact changes; any new sidecar artifact would require an explicit contract-set amendment — the boundary is operationally defined by the exhaustive output list
- [ ] CHK040 Does the spec define what happens to the report-only boundary if a future feature reads the semantic gate's evaluation report and uses it to make a routing decision — i.e. is the boundary defined at the point of production, the point of consumption, or both? [Completeness, Gap] — ⚠ OPEN: boundary defined at production only; consumption side not addressed — STILL OPEN at impl-time: FR-033 is production-side only; consumption by future features is deferred; no normative constraint on future consumers

---

## Migration and Rollback Requirements

- [ ] CHK041 Does the Migration Plan in design.md specify the order of tasks sufficiently to determine which tasks must be completed before others — e.g. does adding the sidecar contract precede the semantic quality module? [Completeness, Spec §Assumptions (design.md Migration Plan)] — ⚠ OPEN: design.md Migration Plan is referenced informally; ordering constraints are not normative in this spec — STILL OPEN at /speckit.tasks: Plan §Source-Code tree is ordered by dependency (contracts before gate modules before writer modules before tests) but task dependency arrows are not normative until tasks.md is generated
- [ ] CHK042 Is the rollback procedure described as "remove the optional semantic truth/report fields and semantic-quality evaluator wiring" defined precisely enough to execute — e.g. does it enumerate which files to remove/revert? [Completeness, Spec §Assumptions (design.md Migration Plan)] — ⚠ OPEN: rollback is referenced informally; no file-level enumeration — STILL OPEN at impl-time: Plan §Source-Code enumerates all new/changed files, which serves as an implicit rollback list, but no normative rollback procedure is defined
- [x] CHK043 Does the spec confirm that rollback leaves all pre-feature `evaluation_document.json` and `evaluation_run_summary.json` artifacts valid without any re-generation step? [Completeness, Spec §FR-019, §Assumptions (design.md Migration Plan)] — ✓ FR-019
- [x] CHK044 Is there a requirement that the `contract_set_version` bump from `1.2.0` to `1.3.0` (FR-031) is accompanied by backward-compatible validator logic — i.e. the v1.3.0 validator still accepts existing v1.2.0 artifacts? [Completeness, Spec §FR-031, §FR-019] — ✓ FR-019 / FR-031
- [ ] CHK045 Does the spec define what "additive rollback" means in terms of `AMENDMENTS.md` — is a rollback required to add an amendment entry recording the reversal, or may it simply remove the v1.3.0 entry? [Completeness, Gap] — ⚠ OPEN: no AMENDMENTS.md rollback procedure defined — STILL OPEN at impl-time: R-022.10 defines the AMENDMENTS.md entry format for v1.3.0 but does not address how to record a rollback or reversal entry
- [x] CHK046 Is the minor-version bump rationale (1.2.0 → 1.3.0 for an additive optional contract) consistent with the amendment policy described in `contracts/stage1_vendor_identity/AMENDMENTS.md` — or does the spec assume a policy that has not been confirmed? [Assumption, Spec §FR-031] — ⚠ OPEN: spec asserts "minor bump" without confirming against AMENDMENTS.md versioning policy — ✓✓ Schema-Amendments §proposed AMENDMENTS.md entry: "Type: Minor (additive only; backward-compatible)"; R-022.10 confirms same level-two heading format as v1.1.0 and v1.2.0 entries; R-022.5: "copy-then-edit procedure already established by v1.0.0 → v1.1.0 → v1.2.0 precedent"
- [ ] CHK047 Does the spec define whether this feature may introduce changes to `pyproject.toml` (e.g. new dependencies or entry-point registrations) and if so, does rollback of those changes revert the installed package to its prior state? [Completeness, Gap] — ⚠ OPEN: no requirement on pyproject.toml changes or rollback — STILL OPEN at impl-time: Plan §Technical Context "No new pinned dependency" and "No new top-level package, no new CLI entry point" imply minimal pyproject.toml changes; but what changes ARE permitted (e.g. adding test paths) is not stated

---

## Scope Conflicts and Ambiguities

- [x] CHK048 Is there a potential conflict between FR-026 (canonical home for promoted fixtures is `tests/stage1_semantic_quality/`) and FR-027 (validator treats noncanonical-suffix folders as calibration material not as scored corpus) — do both requirements cover the same folder set, and are they consistent? [Conflict, Spec §FR-026, §FR-027] — ✓ FR-026 / FR-027
- [x] CHK049 Does the spec resolve whether adding `semantic_table_truth.json` schema to the contract set under `contracts/stage1_vendor_identity/` (FR-031 / Clarifications Q14) is considered a "canonical artifact schema change" that itself requires the amendment process defined in `contracts/stage1_vendor_identity/AMENDMENTS.md`, or is it self-authorizing? [Ambiguity, Spec §FR-031] — ✓ FR-031 / Q14
- [x] CHK050 Is there a potential conflict between FR-028 (MUST NOT rewrite features 019/020/021 in place) and the need to wire the semantic gate inside the existing evaluator — does "in place" cover the evaluator as well as the preprocessing/extraction/routing modules? [Ambiguity, Spec §FR-028] — ✓ FR-028
- [x] CHK051 Does the spec clarify whether the `tests/stage1_semantic_quality/` corpus root is within or outside the scope of the validator's `validate corpus` subcommand — i.e. would running `python -m dartwing_ocr.validator validate corpus tests/stage1_semantic_quality/` be valid after this feature lands? [Completeness, Spec §FR-026, §FR-027] — ⚠ OPEN: FR-027 defines exclusion rules for noncanonical folders but does not address whether the validator subcommand is extended to the new corpus root — ✓✓ Validator-CLI-Contract §validate corpus: behavior applies to any `<corpus-root>` argument; Quickstart Path 5 shows `python -m dartwing_ocr.validator validate corpus tests/stage1_semantic_quality/` as a valid invocation; the Q23 allowlist applies uniformly to both roots
- [x] CHK052 Is there potential overlap between the Out of Scope exclusion of "regenerating committed corpus baselines" and the need to add fixture documents to `tests/stage1_semantic_quality/` — does adding new fixtures constitute "regenerating" baselines or is it clearly distinct? [Ambiguity, Spec §Out of Scope, §FR-026] — ✓ FR-026 / Out of Scope
- [x] CHK053 Does FR-031 specify a cap on how many new optional fields may be added to `evaluation_document.json` and `evaluation_run_summary.json` — or is the cap only that no existing field may be changed? [Completeness, Spec §FR-031] — ✓ FR-031
- [x] CHK054 Is there a potential conflict between FR-033 (semantic verdict MUST NOT drive runtime behavior) and SC-004 (a failing semantic verdict is "reported as not passed in 100% of cases") — could a consumer treat the "not passed" report as a runtime gate? [Conflict, Spec §FR-033, §SC-004] — ✓ FR-033 / SC-004
- [x] CHK055 Does the spec address whether the semantic gate module MUST be skipped entirely (not imported, not instantiated) when a document has no sidecar — or is it acceptable for the gate code to run and short-circuit internally? [Completeness, Spec §FR-007, §FR-005] — ⚠ OPEN: FR-007 says the sidecar's presence is the opt-in but does not specify whether the module is skipped or short-circuits — ✓✓ MI-18 + Data-Model §13 state transitions: sidecar absent → status=not_applicable, no semantic_table_quality object written; gate returns immediately with not_applicable result (short-circuit acceptable); MI-2 determinism is satisfied either way since same inputs → same output; no module-load prohibition (MI-1 covers Paddle import only)
- [ ] CHK056 Is there a tension between US4 AS5 ("reports 'not applicable' — never infers a semantic pass") and any future consumer that might treat absence of a failure as an implicit pass — and does the spec require defensive language in the report schema or documentation to prevent that misinterpretation? [Coverage, Spec §US4, §FR-025] — ⚠ OPEN: no defensive language or schema comment requirement to prevent misinterpretation — STILL OPEN at impl-time: Schema-Amendments §evaluation_document includes a description comment for `semantic_table_quality_passed`; no normative requirement to add defensive warnings in consumer-facing documentation

---

## Notes

- FR-028's prohibition on "rewriting features 019/020/021 in place" is currently qualified by a parenthetical ("it adds a new, separately-surfaced capability"), but no module-path boundary is specified; implementers need a concrete file-system or package-path criterion to apply the rule consistently.
- The Migration Plan in design.md is informative, not normative; if the ordering of migration steps matters for correctness or rollback safety, those ordering constraints should be encoded as functional requirements or task dependencies in the spec.
- FR-031's "beyond (a) and (b)" clause is the key scope guard for contract changes; SC-010 should cross-reference this FR to ensure the tasks and analyze steps enforce it.

---

## Verification — 2026-05-22 (re-verified against spec.md with Clarifications Q1-Q26)

- Items resolved: 20 / 56
- Items still open: 36
- Open items: CHK001 ("rewrite in place" undefined), CHK002 (no module/package path boundary), CHK003 (no concrete file-system prohibition), CHK005 (no normative FR prohibits Jetson-specific code), CHK008 (per-document folder contract for new corpus root unspecified), CHK010 (local REST API edge case not excluded), CHK011 (standalone CLI subcommand not explicitly excluded), CHK013 (final_structured_payload.json not named in FR-033), CHK018 (labeling-guide.md status not resolved), CHK020 (FR-022 does not name specific run-summary fields), CHK021 (FR-023 does not cite specific promotion thresholds), CHK022 (SC-006 "identical" undefined), CHK024 (FR-024 logic vs. output-value ambiguity), CHK025 (no import-time side-effect requirement), CHK026 (profile/invocation not specified in SC-006), CHK027 (runtime conditions not fixed in US4 AS1), CHK028 (no RunSummary.SCHEMA_VERSION drift guard), CHK030 (semantic_verdict.json not explicitly prohibited), CHK031 (no prohibition on writing to vendor-identity folders), CHK033 (runtime review policy scope undefined), CHK034 (prohibited field paths in pipeline artifacts not named), CHK035 (no deferred-work tracking requirement), CHK036 (stdout run_summary lines not addressed), CHK037 (no stdout non-regression requirement), CHK038 (no exit-code requirement), CHK039 ("report-only" not operationally defined), CHK040 (consumption-side boundary not addressed), CHK041 (migration task ordering not normative), CHK042 (rollback not enumerated), CHK045 (AMENDMENTS.md rollback procedure undefined), CHK046 (minor-bump policy not confirmed), CHK047 (pyproject.toml changes not addressed), CHK051 (validate corpus subcommand scope unaddressed), CHK055 (gate module skip vs. short-circuit unspecified), CHK056 (no defensive language requirement)

---

## Verification — 2026-05-23 (re-verified post /speckit.plan)

- Items resolved this round: 22
- Items still open at impl-time: 15
- Open items: CHK001 ("rewrite in place" still not operationally defined as a phrase), CHK005 (no normative FR prohibits Jetson-specific code), CHK021 (FR-023 thresholds unspecified), CHK025 (no normative import-time side-effect prohibition), CHK028 (no RunSummary.SCHEMA_VERSION drift guard beyond the controlled bump), CHK033 (runtime review policy documentation scope undefined), CHK035 (no deferred-work tracking mechanism required), CHK037 (no stdout run_summary non-regression requirement), CHK038 (no evaluator exit-code requirement for failing semantic verdict), CHK040 (consumption-side report-only boundary not addressed), CHK041 (migration task ordering stays at /speckit.tasks), CHK042 (rollback file list implicit but not normative), CHK045 (AMENDMENTS.md rollback procedure undefined), CHK047 (pyproject.toml change scope and rollback unaddressed), CHK056 (no defensive schema/doc language requirement)
- Cumulative resolved: 41 / 56
