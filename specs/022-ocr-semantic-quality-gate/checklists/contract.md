# Contract Requirements Quality Checklist: OCR Semantic Quality Gate

**Purpose**: Validate that the contract and schema requirements — covering the sidecar JSON Schema, contract-set versioning, additive-change boundaries, backward compatibility, and folder-contract membership — are complete, unambiguous, and sufficient to author the governed machine-readable artifacts without implementer guesswork.
**Created**: 2026-05-22
**Feature**: [spec.md](../spec.md)

---

## Contract Definition Completeness

- [x] CHK001 Does the spec specify where the `semantic_table_truth.json` JSON Schema file will live within `contracts/stage1_vendor_identity/v1.3.0/` — i.e. its exact filename — or is the file path left for the implementer to invent? [Completeness, Spec §FR-031 / Spec §Clarifications Q14] — ⚠ OPEN: FR-031 and Q14 name the contract-set directory but not the exact filename for the sidecar schema — ✓✓ Schema-Amendments §Schemas added (filename `semantic_table_truth.schema.json` specified verbatim); Plan §Project-Structure (`contracts/stage1_vendor_identity/v1.3.0/semantic_table_truth.schema.json`)
- [x] CHK002 Does the spec define whether the `semantic_table_truth.json` JSON Schema must declare a `$schema` keyword and which JSON Schema draft is targeted, consistent with the existing contract set's use of Draft 2020-12? [Completeness, Spec §FR-031] — ⚠ OPEN: spec does not require a specific `$schema` declaration or JSON Schema draft for the sidecar schema — ✓✓ Schema-Amendments §semantic_table_truth.schema.json (verbatim JSON Schema opens with `"$schema": "https://json-schema.org/draft/2020-12/schema"` — Draft 2020-12 confirmed)
- [x] CHK003 Does the spec state whether the new 1.3.0 contract set includes a `contract_set.json` metadata file that lists `semantic_table_truth.json` as a member, or whether membership is inferred from directory presence? [Completeness, Spec §FR-031] — ⚠ OPEN: FR-031 references `contract_set_version` bumping to 1.3.0 but does not specify whether `contract_set.json` must be updated to enumerate the new member — ✓✓ Schema-Amendments §contract_set.json delta (verbatim before/after showing `semantic_table_truth.schema.json` added to the `schemas` list and version bumped to `"1.3.0"`)
- [x] CHK004 Is the `AMENDMENTS.md` entry format for the minor version bump 1.2.0→1.3.0 described precisely enough — e.g. required fields such as date, bump type, changed artifacts, rationale — or is the entry format left implicit? [Completeness, Spec §FR-031 / Spec §Clarifications Q14] — ⚠ OPEN: FR-031 and Q14 require an `AMENDMENTS.md` entry but do not specify the required fields or format of that entry — ✓✓ Schema-Amendments §Proposed AMENDMENTS.md entry (copy-ready verbatim entry with date, type, summary bullets per changed schema, justification, spec link, validator confirmation command; format follows prior v1.1.0 and v1.2.0 conventions per R-022.10)
- [x] CHK005 Does the spec define whether a machine-readable contract validator (the existing `python -m dartwing_ocr.validator`) must be updated to recognise the new 1.3.0 contract set version, or whether version detection is outside this feature's scope? [Completeness, Spec §FR-031] — ⚠ OPEN: FR-031 specifies the version bump but does not state whether the existing validator must be updated to recognize 1.3.0 — ✓✓ Plan §Source-Code (`contract_versions.py` CHANGED — bump `CURRENT_CONTRACT_SET_VERSION` 1.2.0→1.3.0; v1.2.0 loader retained for backward-compat per R-022.9); Validator-CLI-Contract §Notes (validator awareness confirmed)
- [x] CHK006 Does the spec define whether the `semantic_table_truth.json` schema must enforce the `row_id` uniqueness constraint (Clarifications Q11) at the JSON Schema level (e.g. using `uniqueItems` on a derived array or a `unevaluatedProperties` guard), or whether uniqueness is enforced only by application logic? [Completeness, Spec §FR-004 / Spec §Clarifications Q11] — ⚠ OPEN: FR-004 requires uniqueness checking but does not specify JSON Schema vs. application-logic enforcement — ✓✓ Schema-Amendments §semantic_table_truth.schema.json (note: "`row_id` uniqueness across the sidecar is enforced at validator layer (jsonschema does not natively support array-uniqueness-by-key)"); Data-Model §2 Sidecar-level uniqueness (validator layer performs the additional check after schema validation)
- [x] CHK007 Does the spec define the JSON Schema representation of the "normalized decimal string without a currency symbol" constraint (Clarifications Q9) — e.g. a `pattern` keyword — rather than describing it only in prose? [Completeness, Spec §FR-002 / Spec §Clarifications Q9] — ⚠ OPEN: Q9 describes the constraint in prose only; no `pattern` keyword or regex is specified for the sidecar schema — ✓✓ Schema-Amendments §semantic_table_truth.schema.json (`unit_price` and `amount` both have `"pattern": "^\\d+\\.\\d{2}$"` — JSON Schema keyword encoding confirmed)
- [x] CHK008 Does the spec define the JSON Schema representation of the `row_id` non-empty string constraint (Clarifications Q11) — e.g. `"minLength": 1` — or leave it as prose only? [Completeness, Spec §FR-002 / Spec §Clarifications Q11] — ⚠ OPEN: non-empty constraint is prose-only; JSON Schema encoding not specified — ✓✓ Schema-Amendments §semantic_table_truth.schema.json (`row_id`: `{"type": "string", "minLength": 1}` — JSON Schema keyword confirmed)
- [x] CHK009 Does the spec define whether the additive semantic-quality fields on `evaluation_document.json` and `evaluation_run_summary.json` (FR-017, FR-018) are governed by a separate JSON Schema inside the contract set, or whether they are additions to existing schemas? [Completeness, Spec §FR-031] — ⚠ OPEN: FR-031 states only the sidecar schema is added; whether evaluation-report schemas are updated inside the contract set or are evaluator-internal is not specified — ✓✓ Schema-Amendments §Schemas changed (evaluation_document.schema.json and evaluation_run_summary.schema.json are explicitly changed in-place within the v1.3.0 contract set directory — they ARE governed schemas, not evaluator-internal; Plan §Project-Structure lists them under `contracts/stage1_vendor_identity/v1.3.0/`)
- [x] CHK010 Does the spec specify whether the `document_pass_fail.semantic_table_quality_passed` field (Clarifications Q12, FR-025) is added to an existing `document_pass_fail` JSON Schema or to a new schema, and which artifact's contract governs it? [Completeness, Spec §FR-025 / Spec §Clarifications Q12] — ⚠ OPEN: FR-025 defines the field semantics but does not specify which governed schema file covers it — ✓✓ Schema-Amendments §evaluation_document.schema.json (verbatim delta shows `semantic_table_quality_passed` added to the existing `document_pass_fail` object in `evaluation_document.schema.json`; same schema file, additive change)
- [x] CHK011 Is the "zero rows" rejection constraint for `semantic_table_truth.json` (Edge Cases) expressed as a JSON Schema keyword — e.g. `"minItems": 1` on the rows array — or left as application-logic only? [Completeness, Spec §Edge Cases] — ⚠ OPEN: zero-row rejection is stated as an edge case in prose; no JSON Schema keyword encoding is specified — ✓✓ Schema-Amendments §semantic_table_truth.schema.json (`"rows": {type: array, minItems: 1}` — JSON Schema keyword `minItems: 1` confirmed)

---

## Versioning & Amendment Requirements

- [x] CHK012 Is the version bump described as "minor" (1.2.0→1.3.0) consistent with the project's SemVer policy for additive non-breaking changes, and does the spec cite that policy or the `AMENDMENTS.md` amendment checklist as the authority? [Consistency, Spec §FR-031 / Spec §Clarifications Q14] — ✓ FR-031 / Q14: FR-031 describes the bump as "a minor bump from `1.2.0` to `1.3.0`" and references `AMENDMENTS.md`; additive-only characterization is consistent with SemVer policy documented in CLAUDE.md
- [x] CHK013 Does the spec define what "advancing the contract_set_version" means operationally — i.e. which file(s) must have their version string updated, in which field — rather than only naming the target version? [Completeness, Spec §FR-031] — ⚠ OPEN: FR-031 names "1.3.0" as the target but does not enumerate which files must be updated (e.g. `contract_set.json` version field) — ✓✓ Schema-Amendments §contract_set.json delta (verbatim `contract_set_version` field updated from `"1.2.0"` to `"1.3.0"`); Plan §Source-Code (`contract_versions.py` — bump `CURRENT_CONTRACT_SET_VERSION`); Research R-022.5 (procedure: copy v1.2.0 → v1.3.0, then edit four files + update contract_set.json)
- [x] CHK014 Does the spec state whether the existing frozen v1.0.0 and v1.1.0 contract snapshots remain untouched by the 1.3.0 bump, or whether any backport or reference update is required? [Completeness, Spec §FR-031] — ✓ FR-031: FR-031 states "Any broader contract change requires an explicit contract-set amendment"; the frozen snapshots policy is established in CLAUDE.md ("v1.0.0 and v1.1.0 snapshots remain frozen for compatibility") — no backport required
- [ ] CHK015 Does the spec require that the `AMENDMENTS.md` entry classify the 1.2.0→1.3.0 bump as additive-only, and does it specify who is authorised to approve such an entry? [Completeness, Gap] — ⚠ OPEN: spec does not specify who must approve the AMENDMENTS.md entry — STILL OPEN at impl-time: Schema-Amendments §Proposed AMENDMENTS.md entry includes `"Type": Minor (additive only; backward-compatible)` — bump type is classified — but approval authority (who signs off on AMENDMENTS.md) is still not specified by any planning artifact
- [x] CHK016 Is the `contract_set_version = "1.3.0"` value in the spec consistent with the project's current pinned version `"1.2.0"` documented in the codebase's CLAUDE.md, so there is no ambiguity about the base from which the bump is made? [Consistency, Spec §FR-031] — ✓ FR-031: CLAUDE.md documents current version as `1.2.0`; FR-031 explicitly states "advancing `contract_set_version` by a minor bump from `1.2.0` to `1.3.0`" — base and target are unambiguous

---

## Backward Compatibility

- [x] CHK017 Is the requirement that pre-feature `evaluation_document.json` reports "still validate" (FR-019) defined against a specific schema — e.g. the 1.3.0 evaluation-document schema — or left as an informal statement that could be satisfied by schema-less acceptance? [Clarity, Spec §FR-019] — ⚠ OPEN: FR-019 says pre-feature reports "MUST still validate" but does not specify which schema version or which validator command produces that verdict — ✓✓ Evaluator-Output-Contract §Backward-compat readers (pre-feature artifact MUST validate against v1.3.0 `evaluation_document.schema.json` without error — specific schema version stated); Validator-CLI-Contract §validate folder and §validate artifact (the validator uses v1.3.0 schemas when `CURRENT_CONTRACT_SET_VERSION == "1.3.0"`)
- [x] CHK018 Is the requirement that existing reports remain valid (FR-019, SC-008) expressed as a testable predicate — e.g. "the 1.3.0 schema validator must accept a v1.2.x-era report with no semantic fields, producing zero errors" — so that a test can be written without interpretation? [Measurability, Spec §FR-019 / Spec §SC-008] — ✓ SC-008: SC-008 states "Existing pre-feature `evaluation_document.json` and `evaluation_run_summary.json` artifacts still validate against the report schemas after the feature lands (zero breakage of prior reports)" — a testable zero-error predicate
- [x] CHK019 Does the spec define whether the evaluation report schemas use `additionalProperties: false` or `unevaluatedProperties: false` in a way that would reject pre-feature reports, or does it require these schemas to permit additional properties to preserve backward compatibility? [Completeness, Spec §FR-019] — ⚠ OPEN: FR-019 requires backward compatibility but does not specify the `additionalProperties` policy on the schema — ✓✓ Schema-Amendments §evaluation_document.schema.json (new fields are added as optional properties — they are NOT in the `required` array — so pre-feature reports that lack them remain valid; the schema's outer `additionalProperties` policy is inherited from v1.2.0 and not tightened by this change); Research R-022.9 (two-schema model: write schema enforces full v1.3.0 shape; read path uses v1.2.0 or a permissive read schema)
- [x] CHK020 Does the spec address whether a consumer reading a pre-feature `evaluation_document.json` that lacks `semantic_table_quality` must treat the absent field as "gate did not run" rather than as a data error, and is this consumer contract documented? [Completeness, Spec §Edge Cases] — ✓ Edge Cases / FR-019: the edge case "An existing v1.2 evaluation report is re-read after the feature lands: it remains valid; absence of `semantic_table_quality` is interpreted as 'semantic gate did not run', not as a failure" explicitly documents this consumer contract
- [x] CHK021 Is the backward-compatibility guarantee scoped to validation (schema acceptance) only, or does it also require that evaluation tooling processing old reports does not error, and is the scope explicitly stated? [Clarity, Spec §FR-019] — ⚠ OPEN: FR-019 says reports "MUST still validate" (schema acceptance) but does not address whether evaluation tooling processing old reports must also be error-free — ✓✓ Evaluator-Output-Contract §Backward-compat readers (scope covers BOTH schema validation AND tooling: "No code path may raise an error on the mere absence of these fields in a pre-feature report"); MI-24 (reader MUST interpret absent field as `null`, not raise — tooling error-free requirement is explicit)
- [x] CHK022 Does the spec define whether the 1.2.0→1.3.0 version bump could cause the existing validator's `contract_set_version = "1.2.0"` pin to reject new reports, and if so, does it require a validator update as part of the feature? [Completeness, Spec §FR-031] — ✓ FR-031 / CHK005 context: FR-031 states the version advances; the broader CLAUDE.md validator note plus FR-031's "contract-set amendment" requirement implies validator awareness; this item resolves because SC-008 requires no breakage of existing reports and FR-031 requires the bump — the expectation of a validator update is implicit in the non-regression requirement (SC-008 / SC-006)

---

## Schema Validation Rules

- [x] CHK023 Does the spec require that the `semantic_table_truth.json` schema be used by the validator to gate sidecar acceptance (FR-004), and is the entry point for that validation — i.e. which validator subcommand or API — specified? [Completeness, Spec §FR-004] — ⚠ OPEN: FR-004 requires sidecar acceptance/rejection but does not specify which `python -m dartwing_ocr.validator` subcommand or API entry point performs sidecar validation — ✓✓ Validator-CLI-Contract §validate semantic-truth (new subcommand specified with exact invocation, exit codes, and all six validation steps); Validator-CLI-Contract §validate folder (extended behavior when sidecar is present)
- [x] CHK024 Does the spec require schema validation of the `document_id` match rule (FR-003) separately from JSON Schema structural validation, and is this described as a semantic check rather than a schema check so implementers understand both layers are required? [Clarity, Spec §FR-003] — ✓ FR-003 / FR-004: FR-003 defines the `document_id` match as a separate acceptance predicate; FR-004 requires both structural and `document_id`-match checks for sidecar acceptance — two layers are clearly required
- [x] CHK025 Does the spec specify what error format or message structure the validator must emit when it rejects a sidecar due to a `document_id` mismatch (FR-003) — i.e. must the error name both the expected and observed values? [Completeness, Spec §FR-003 / Spec §US1] — ⚠ OPEN: US1 scenario 2 says the error must "name the mismatch" but does not specify the error message format or whether both expected and observed values must appear — ✓✓ Validator-CLI-Contract §Error message content (exact required substrings specified: declared value AND folder basename must both appear; conforming example given); Data-Model §1 Validator error requirements (error MUST name both the declared `document_id` value and the folder basename)
- [x] CHK026 Does the spec specify what error format the validator must emit when it rejects a sidecar due to a row contract violation (FR-004) — i.e. must the error identify the `row_id` of the offending row? [Completeness, Spec §FR-004 / Spec §US1] — ⚠ OPEN: US1 scenario 3 says the error must "identify the offending row" but does not specify the error format or whether `row_id` must be named — ✓✓ Validator-CLI-Contract §Error message content (row violation error MUST name `row_id` (or array index), failed field, and one-line reason; three conforming examples given); Data-Model §1 Validator error requirements (same requirement)
- [x] CHK027 Does the spec specify what error format the validator must emit when it rejects a sidecar due to duplicate `row_id` values (FR-004, Clarifications Q11), and does it require naming the duplicated `row_id`? [Completeness, Spec §FR-004 / Spec §Clarifications Q11] — ⚠ OPEN: FR-004 requires the validator to reject duplicate `row_id`s with a "row-identifying error" but does not specify that the error must name the duplicated value — ✓✓ Validator-CLI-Contract §Error message content (third example shows duplicate `row_id` error naming the duplicate value and both indices: `"duplicate row_id 'row-2' at index 3 (first seen at index 1)"`)
- [x] CHK028 Is the scope of the "row conforms to the row-truth contract" acceptance predicate (FR-004) defined to include all of: structural JSON Schema validity, mandatory field presence, `row_id` non-empty, and `row_id` uniqueness — so no check is accidentally omitted from the validator? [Completeness, Spec §FR-004] — ✓ FR-004 / Q8 / Q11: FR-004 requires the validator to check row contract conformance and `row_id` uniqueness; FR-002/Q8 defines mandatory fields (`row_id` + row text); Q11 defines non-empty uniqueness — together the four components are covered in spec prose
- [x] CHK029 Does the spec define whether the validator is expected to accept a sidecar that contains additional fields beyond those defined in the schema (e.g. the "optional future metadata" row-region anchors mentioned in FR-002), or whether extra fields cause rejection? [Completeness, Spec §FR-002] — ⚠ OPEN: FR-002 says anchors are "optional future metadata and MUST NOT be required" but does not specify whether the validator accepts or rejects extra/unknown fields — ✓✓ Schema-Amendments §semantic_table_truth.schema.json (`additionalProperties: false` on both the top-level object and the row object — extra fields are rejected; future anchors will require a schema amendment when specified, consistent with the contract-set amendment model)

---

## Folder Contract

- [x] CHK030 Is the per-document folder contract updated to list `semantic_table_truth.json` as an optional member, and does the spec require that the folder-contract artifact under `contracts/stage1_vendor_identity/v1.3.0/` be amended as part of the feature? [Completeness, Spec §FR-031] — ⚠ OPEN: FR-031 states the sidecar schema is added to the contract set but does not explicitly state the folder-contract artifact must also be amended to list the sidecar as an optional member — ✓✓ Schema-Amendments §folder.schema.json delta (verbatim before/after: `semantic_table_truth.json` added as optional entry alongside unchanged mandatory artifacts); Plan §Project-Structure (`folder.schema.json` listed under CHANGED files in v1.3.0)
- [ ] CHK031 Does the spec define whether the folder contract for `tests/stage1_semantic_quality/` (Clarifications Q15, FR-026) follows the same schema as `tests/stage1_vendor_identity/` folders, or whether it has a different permitted artifact set? [Completeness, Spec §FR-026 / Spec §Clarifications Q15] — ⚠ OPEN: FR-026 says the new corpus root "MAY reuse the existing per-document folder discipline where useful" but does not specify whether the folder contract is the same schema or a new one — STILL OPEN at impl-time: Schema-Amendments §folder.schema.json notes the US2 synthetic fixture (`tests/stage1_semantic_quality/inv_001_hard/`) is "exempt from the `source.pdf` mandatory requirement" but does not formally define a separate folder contract schema for `tests/stage1_semantic_quality/`; the exemption mechanism is prose-only
- [ ] CHK032 Does the spec state whether the `tests/stage1_semantic_quality/` corpus root itself needs a corpus-root contract artifact (analogous to whatever governs `tests/stage1_vendor_identity/`), and if so, what that artifact is? [Completeness, Gap] — ⚠ OPEN: no corpus-root contract artifact for `tests/stage1_semantic_quality/` is defined — STILL OPEN at impl-time: no planning artifact (plan.md, research.md, data-model.md, schema-amendments.md) defines a corpus-root manifest for this new directory; the synthetic fixture lands without one
- [x] CHK033 Is the folder naming convention for `tests/stage1_semantic_quality/` per-document subdirectories specified — e.g. must they follow the `inv_XXX_<difficulty>` pattern, or can they use different naming including the `_degraded_body` suffix? [Completeness, Spec §FR-026 / Spec §Clarifications Q15] — ⚠ OPEN: FR-026 and Q15 define the corpus root but do not specify the naming convention for subdirectories within `tests/stage1_semantic_quality/` — ✓✓ Plan §Technical-Context (the synthetic US2 fixture is `inv_001_hard/` — matches the canonical pattern `^inv_\d{3}_(easy|medium|hard)$`); Data-Model §9 `CANONICAL_FOLDER_PATTERN` (the pattern governs both corpus roots uniformly per MI-21 — the same pattern applies to `tests/stage1_semantic_quality/` subdirectories)
- [ ] CHK034 Does the spec define which artifacts are required versus optional in a `tests/stage1_semantic_quality/` per-document folder at the time of promotion, so the promotion criteria are objectively checkable? [Completeness, Spec §FR-026 / Spec §US5] — ⚠ OPEN: FR-026 lists four amendment types for promotion but does not enumerate the required artifact set for a scored `tests/stage1_semantic_quality/` folder — STILL OPEN at impl-time: Schema-Amendments §folder.schema.json notes the US2 fixture exemption from `source.pdf` requirement but does not enumerate the minimum artifact set for a fully promoted scored `tests/stage1_semantic_quality/` folder

---

## Additive-Change Boundary

- [ ] CHK035 Is the additive-only constraint in FR-031 — "beyond (a) the new optional sidecar contract and (b) the optional additive semantic-quality fields on evaluation reports" — precise enough to constitute a merge-gate criterion, or does "additive" require further definition (e.g. no removal, no rename, no type narrowing)? [Clarity, Spec §FR-031] — ⚠ OPEN: FR-031 characterizes the change as "additive" but does not define what operations are prohibited (e.g. no field removal, no type narrowing) in a machine-checkable way — STILL OPEN at impl-time: Schema-Amendments §Proposed AMENDMENTS.md entry classifies it as "additive only; backward-compatible" but does not enumerate prohibited schema operations; the PR reviewer must still apply judgment to assess whether a specific change is additive
- [ ] CHK036 Does the spec define what constitutes a "broader contract change" that would require an explicit contract-set amendment beyond the minor bump, so reviewers can enforce the additive boundary during PR review? [Completeness, Spec §FR-031] — ⚠ OPEN: "broader contract change" is not defined with examples or predicates for PR review — STILL OPEN at impl-time: no planning artifact defines this predicate; reviewer judgment remains required
- [ ] CHK037 Is the constraint that `expected.json` shape must not change (FR-006) stated as a contract requirement that could be validated — e.g. by diffing the `expected.json` JSON Schema between v1.2.0 and v1.3.0 — rather than only as a prose obligation? [Measurability, Spec §FR-006] — ⚠ OPEN: FR-006 is a prose obligation; no machine-checkable schema-diff predicate is specified — STILL OPEN at impl-time: Schema-Amendments §Schemas unchanged confirms `expected.schema.json` is "copied unchanged from v1.2.0" (a prose statement), but the machine-checkable predicate (e.g. SHA comparison of the two schema files) is not specified in any planning artifact
- [x] CHK038 Does the spec define whether adding `document_pass_fail.semantic_table_quality_passed` to the final-payload or evaluation-report schema counts as an additive change that stays within the FR-031 boundary, or whether it requires a separate amendment entry? [Completeness, Spec §FR-025 / Spec §FR-031] — ✓ FR-031 / FR-025: FR-031 explicitly names "(b) the optional additive semantic-quality fields on evaluation reports" as within the boundary; FR-025 defines the field as an additive sibling — no separate amendment entry required
- [x] CHK039 Does the spec define whether the changes to `evaluation_run_summary.json` schema (FR-018) — adding aggregate semantic-quality metrics — are governed by the contract-set version bump or are considered evaluator-internal and not governed by the machine-readable contract set? [Completeness, Spec §FR-018 / Spec §FR-031] — ⚠ OPEN: FR-031 enumerates "(b) the optional additive semantic-quality fields on evaluation reports" within scope, but does not explicitly state whether `evaluation_run_summary.json` changes are machine-readable contract-governed or evaluator-internal only — ✓✓ Schema-Amendments §Schemas changed (evaluation_run_summary.schema.json explicitly listed under "Schemas changed" in the v1.3.0 contract set — it IS machine-readable contract-governed); Plan §Project-Structure (`evaluation_run_summary.schema.json` listed as CHANGED under `contracts/stage1_vendor_identity/v1.3.0/`)

---

## Ambiguities & Conflicts

- [x] CHK040 Is there a potential conflict between FR-019 ("evaluation report schemas MUST remain tolerant of existing reports") and FR-031 ("schema MUST be added to the governed contract set, advancing to 1.3.0") — specifically, does the spec clarify that the new 1.3.0 evaluation schema must be a strict superset of the 1.2.x schema with no removed fields, no narrowed types, and no removed `additionalProperties` permissions? [Conflict, Spec §FR-019 / Spec §FR-031] — ⚠ OPEN: SC-008 requires zero breakage of prior reports and FR-019 requires schema tolerance, but the spec does not define "strict superset" as the mechanism or enumerate prohibited schema operations — ✓✓ Schema-Amendments §evaluation_document and §evaluation_run_summary (both show explicit BEFORE/AFTER diffs — only additions, no removals, no type narrowings; the diff is the machine-checkable evidence); Research R-022.5 (copy-then-edit procedure preserves all existing fields); Evaluator-Output-Contract §Backward-compat readers ("both new fields are optional in the schema")
- [x] CHK041 Does the spec reconcile the statement that the sidecar "MUST be added to the governed machine-readable contract set under `contracts/stage1_vendor_identity/`" (FR-031) with the fact that `tests/stage1_semantic_quality/` is a separate corpus root — i.e. does the sidecar contract govern both corpus roots or only one? [Ambiguity, Spec §FR-031 / Spec §Clarifications Q15] — ✓ FR-031 / FR-026: FR-031 places the sidecar schema in `contracts/stage1_vendor_identity/` as the governed contract; FR-026 establishes `tests/stage1_semantic_quality/` as a corpus root that reuses existing folder discipline where useful — the contract governs the sidecar artifact shape regardless of corpus root, so both corpus roots are governed by the same sidecar schema
- [ ] CHK042 Is it specified whether the `AMENDMENTS.md` entry for 1.3.0 must be committed in the same PR as the schema files, or whether it may be committed independently, so there is no window where the contract set is in an inconsistent state? [Completeness, Gap] — ⚠ OPEN: no atomicity requirement is specified for the AMENDMENTS.md entry and schema files — STILL OPEN at impl-time: Research R-022.10 and Schema-Amendments §Proposed AMENDMENTS.md entry give the content but do not specify atomicity (same commit/PR vs. independent); this is a PR-hygiene decision not resolved by planning artifacts
- [x] CHK043 Does the spec clarify whether "minor bump" (FR-031, Clarifications Q14) implies that the contract-set validator must continue to accept 1.2.x artifacts when running in 1.3.0 mode, or whether cross-version validation is undefined? [Ambiguity, Spec §FR-031 / Spec §Clarifications Q14] — ⚠ OPEN: FR-031 and Q14 name the bump but do not define cross-version validation behavior — ✓✓ Research R-022.9 (v1.2.0 schema loader retained and importable for backward-compat reads; v1.2.0 schemas remain available for validating pre-feature reports); MI-24 (absent field interpreted as `null` — cross-version read tolerance is an explicit behavior, not undefined)
- [x] CHK044 Is it unambiguous whether the `semantic_table_truth.json` JSON Schema is the only new schema file added at 1.3.0, or whether the evaluation-report schema changes also require new or updated versioned schema files under `contracts/stage1_vendor_identity/v1.3.0/`? [Ambiguity, Spec §FR-031] — ⚠ OPEN: FR-031 names the sidecar schema explicitly but is silent on whether evaluation-report schema files are added or updated in the versioned contract directory — ✓✓ Schema-Amendments §Schemas changed (three existing schema files are explicitly listed as CHANGED in v1.3.0: `evaluation_document.schema.json`, `evaluation_run_summary.schema.json`, `folder.schema.json`; they ARE updated versioned files in `contracts/stage1_vendor_identity/v1.3.0/`); Schema-Amendments §Schemas unchanged (six files copied unchanged)

---

## Notes

- This checklist gates whether the contract and versioning requirements are precise enough to author the governed machine-readable artifacts (JSON Schemas, `contract_set.json`, `AMENDMENTS.md`) without inventing details not specified.
- The highest-risk gaps are: the absence of a specified filename for the sidecar JSON Schema within the v1.3.0 directory (CHK001), the lack of a machine-testable backward-compatibility predicate (CHK018), and the ambiguity about which schemas are actually governed by the contract-set version bump versus being evaluator-internal (CHK039, CHK044).
- The additive-boundary definition (CHK035–CHK039) is critical for PR review: "additive" must be operationally defined so that changes to existing evaluation report schemas can be evaluated mechanically, not subjectively.

---

## Verification — 2026-05-22 (re-verified against spec.md with Clarifications Q1-Q26)

- Items resolved: 8 / 44
- Items still open: 36
- Open items: CHK001 (sidecar schema filename not specified), CHK002 (JSON Schema draft not specified), CHK003 (contract_set.json membership update not specified), CHK004 (AMENDMENTS.md entry format not specified), CHK005 (validator update requirement not stated), CHK006 (row_id uniqueness enforcement layer — JSON Schema vs. app logic — not specified), CHK007 (decimal string `pattern` keyword not specified), CHK008 (row_id `minLength` JSON Schema keyword not specified), CHK009 (evaluation-report schema governance — contract set vs. evaluator-internal — not specified), CHK010 (document_pass_fail schema ownership not specified), CHK011 (zero-rows JSON Schema `minItems` not specified), CHK013 (which files to update for version-string advance not specified), CHK015 (AMENDMENTS.md approval authority not specified), CHK017 (backward-compat validation predicate not tied to a specific schema), CHK019 (additionalProperties policy not specified), CHK021 (backward-compat scope — schema vs. tooling — not stated), CHK023 (validator subcommand/API entry point for sidecar validation not specified), CHK025 (document_id mismatch error format not specified), CHK026 (row violation error format not specified), CHK027 (duplicate row_id error format not specified), CHK029 (extra-field validator behavior — accept vs. reject — not specified), CHK030 (folder-contract artifact amendment not explicitly required), CHK031 (semantic_quality folder contract — same as vendor-identity or new — not specified), CHK032 (corpus-root contract artifact for tests/stage1_semantic_quality/ not defined), CHK033 (subfolder naming convention for tests/stage1_semantic_quality/ not specified), CHK034 (required vs. optional artifacts at promotion not enumerated), CHK035 (additive boundary — prohibited operations — not machine-checkable), CHK036 (broader contract change predicate for PR review not defined), CHK037 (expected.json shape-change predicate not machine-checkable), CHK039 (evaluation_run_summary.json governance — contract-set vs. evaluator-internal — not specified), CHK040 (strict-superset mechanism not defined), CHK042 (AMENDMENTS.md + schema files atomicity not specified), CHK043 (cross-version validation behavior not defined), CHK044 (evaluation-report schema files in versioned contract directory — presence/update not specified)

---

## Verification — 2026-05-23 (re-verified post /speckit.plan)

- Items resolved this round: 26
- Items still open at impl-time: 8
- Open items: CHK015 (AMENDMENTS.md approval authority — not addressed by any planning artifact), CHK031 (tests/stage1_semantic_quality/ folder contract — source.pdf exemption is prose-only; no separate schema defined), CHK032 (corpus-root manifest for tests/stage1_semantic_quality/ — not defined), CHK034 (minimum artifact set for scored promotion — not enumerated), CHK035 (additive-boundary prohibited-operation list — still reviewer judgment), CHK036 (broader-contract-change predicate — still undefined), CHK037 (expected.json shape-change predicate — no machine-checkable schema-diff predicate specified), CHK042 (AMENDMENTS.md + schema atomicity in same commit/PR — not specified)
- Cumulative resolved: 36 / 44  (Round 1: 10, Round 2: 26)
